# SWM 控制台 coze-loop 化改造计划（页面编号重排）

## 背景与决策（已与用户确认）

将 coze-loop 的 Prompt 工程 + 评测页面接入 SWM 智能体工厂控制台，"以 coze-loop 为核心"还原其原始布局，采用平台灰阶线框稿画法。经多轮确认，锁定如下边界：

- **保留**：SWM-01/02/03（人设与画像，不动）
- **新画 5 页 coze-loop**：Prompt 开发列表 / Prompt 详情·Playground / 评测集 / 评估器 / 实验
- **观测页不画**（Trace/统计/自动化任务，观测职责归 MC/OM，不入 SWM）
- **只改页面编号**，后端模块编号 M-SWM-04~07 保持现状跳号（03 留缺口），**不动** RTM/架构图/概要设计/SRS/SDD/模块拆分 xlsx
- 侧边栏：平台两级常驻树样式 + coze-loop 分组命名，coze-loop 的模型/应用/标签/SDK&API 4 组不纳入
- 一次性全做完（线框 + 文档联动）

## 最终页面编号（12 页，5 组）

| 新编号 | 页面 | 来源 | 对应模块 |
|---|---|---|---|
| SWM-01 | 智能体总览首页 | 保留 | M-SWM-00 |
| SWM-02 | 人设标签管理 | 保留 | M-SWM-01 |
| SWM-03 | 按账号画像自动生成提示词 | 保留 | M-SWM-07 |
| **SWM-04** | **Prompt 开发列表** | 新画(coze-loop) | M-SWM-02 |
| **SWM-05** | **Prompt 详情·Playground** | 新画(coze-loop) | M-SWM-02 |
| **SWM-06** | **评测集** | 新画(coze-loop) | M-SWM-06 |
| **SWM-07** | **评估器** | 新画(coze-loop) | M-SWM-06 |
| **SWM-08** | **实验** | 新画(coze-loop) | M-SWM-06 |
| SWM-12 | 蜂群记忆管理 | 保留(原SWM-11改号) | M-SWM-04 |

一级分组（侧边栏）：① 人设与画像(SWM-01~03) ② Prompt 工程(SWM-04~05) ③ 评测(SWM-06~08) ④ 记忆(SWM-12)。

> 注：SWM-09~11 编号留空（观测页不画，但编号段预留不连续使用，避免和未来可能的观测页冲突；SWM-12 记忆顺位靠后是因为按"人设→Prompt工程→评测→记忆"业务流排序）。

## 执行步骤

### 阶段一：线框图（认知作战平台-线框图.pen）

**1. 删除 7 个旧页面**（已确认节点 id）：
- `lxxxy`(SWM-04模板库)、`S0f2A`(SWM-05版本)、`i1s6wl`(SWM-06 Playground旧)、`IuV76`(SWM-07版本对比)、`f1I3Pm`(SWM-08构建下发)、`iM3MP`(SWM-09评测集)、`l9rq4`(SWM-10评测实验)

**2. 重做 SWM 侧边栏组件 `Ksi5E`**（reusable）：
- 按平台 §9.2 两级常驻树规范（一级小字分组 + 二级大字页 + 灰图标占位符）
- 5 组 9 项：人设与画像(01总览/02人设/03画像) / Prompt 工程(04开发列表/05详情Playground) / 评测(06评测集/07评估器/08实验) / 记忆(12记忆)
- 因实例化时只覆盖高亮，组件改完后所有引用它的页面（含保留的01/02/03/记忆）需重挂 descendants

**3. 新画 5 页 coze-loop 页面**（统一 1440 宽，灰阶占位符，布局忠实 coze-loop）：

| 页面 | 布局（已探明） |
|---|---|
| SWM-04 Prompt 开发列表 | list-table：标题区 + 工具栏(搜索Prompt Key/名称 + 创建人下拉 + 新建按钮) + 表格9列(Prompt Key/名称/描述/最新版本Tag/最近提交人/提交时间/创建人/创建时间/操作[详情\|调用记录]) + 分页 |
| SWM-05 Prompt 详情·Playground | 三栏IDE：标题栏(返回+名称+状态徽章+对比模式/版本记录/提交新版按钮) + Tab(编排/观测/评测) + 左中(Prompt模板编辑器：System消息富文本+变量高亮{{query}}+添加消息) + 右(常用配置折叠组[模型配置下拉+参数滑块:随机性/TopP/惩罚/最大长度] + Prompt变量 + 预览与调试聊天框+运行) |
| SWM-06 评测集 | list-table：标题 + 工具栏(搜索+新建) + 表格11列(ID/名称/列名/数据项/最新版本/描述/更新人/更新时间/创建人/创建时间/操作) + 分页 |
| SWM-07 评估器 | list-table+Tab：标题 + Tab(自建/预置评估器) + 工具栏(搜索+类型下拉+新建) + 表格9列(名称/类型/最新版本/描述/更新人/更新时间/创建人/创建时间/操作[详情\|复制]) + 分页 |
| SWM-08 实验 | list-table+Tab：标题 + Tab(实验列表/离线实验模板) + 工具栏(帮助文档/过滤器/实验对比/新建实验) + 表格15列(实验名称/类型/对象类型/对象/数据/模板/状态/状态详情/得分/描述/触发方式/创建人/创建时间/结束时间/操作) + 分页 |

布局坐标：放在 SWM 区（y≈18474 起，按菜单行排列），删除旧页腾出的空间复用。

**4. 更新保留页面的侧边栏引用**：SWM-01(`vBRJg`)/02(`KFhRt`)/03(`EGlW6`)/记忆(`ZgtME`) 重挂新 `Ksi5E` 的 descendants（高亮对应项）。

**5. 验证**：每页 `snapshot_layout(problemsOnly:true)` 查溢出/折叠；`get_screenshot` 截图自查视觉（灰阶一致、无重叠、占位符规范）。

### 阶段二：文档联动（仅页面相关）

**6. 改 `系统页面清单.md`**：
- §1.3 门户拓扑表：SWM 行 "4 一级菜单/11 页" → "5 一级菜单/12 页"；合计 62→63 业务页
- §6 SWM 智能体工厂整节重写：6.1 总览/6.2 人设与画像(保留)/6.3 Prompt 工程(SWM-04/05，coze-loop)/6.4 评测(SWM-06/07/08，coze-loop)/6.5 记忆(SWM-12)；每页表格（页面/职责/角色/对应模块）
- §8 页面与模块映射总表：SWM 行页数 11→12、模块列表更新
- §9 待补充事项第4条（coze-loop 页面边界）更新为"已落地"

**7. 追加 `版本变动记录/版本变动记录.md`**：
- 主表顶部加一行（版本列 `—`，因页面清单/线框无正式版本号；日期 2026-07-16；修改文档=系统页面清单+线框图；摘要加粗"SWM 控制台 coze-loop 化"+①删7旧页②新画5页③侧边栏重做④编号重排明细）
- 末尾加 `## SWM 控制台 coze-loop 化变更明细（2026-07-16）`：结构调整说明 + "旧编号/名称/新编号"三列表 + 不变项说明（模块编号不动）

### 阶段三：收尾

**8. 保存 pen 文件**：`save 认知作战平台-线框图.pen` 确保落盘。

## 不做（明确边界）

- 不改 RTM xlsx、架构图.md、概要设计说明.md、SRS SWM 分册、SWM SDD、模块拆分 xlsx 的模块/需求/功能点编号
- 不新建 R-SWM-006/M-SWM-08（观测不入需求）
- M-SWM-03 保持废止缺口，不重新启用
- 不画 coze-loop 的模型管理/应用注册/标签管理/SDK&API/观测 5 组页面

## 风险与验证

- **侧边栏组件改动影响所有 SWM 页**：改 `Ksi5E` 后必须重挂 4 个保留页的 descendants，否则高亮错位——每页 batch_get 复查
- **表格列数多（实验15列）易溢出**：1440 宽内容区，15 列需压缩列宽或横向滚动提示，snapshot_layout 重点查
- **三栏IDE（SWM-05）复杂**：分批 batch_design，每批独立作用域，跨批用返回的真实 id