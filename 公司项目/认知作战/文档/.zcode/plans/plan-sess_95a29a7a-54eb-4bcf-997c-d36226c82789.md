## 目标

将散落在根目录的 6 个详细设计（SDD）文档迁入新建的 `SDD/` 子目录，与已有的 `SRS/` 目录命名惯例对齐。

## 迁移目标目录

`/home/sjg/obs/shijianguo/公司项目/认知作战/文档/SDD/`

迁移文件（6 个，`git mv` 保留历史）：
- `软件详细设计说明-DC子系统.md`
- `软件详细设计说明-OM子系统.md`
- `软件详细设计说明-COM子系统.md`
- `软件详细设计说明-SWM子系统.md`
- `软件详细设计说明-MC子系统.md`
- `软件详细设计说明-OCC子系统.md`

## 引用同步改动清单

### 1. 必须改（真实路径链接 / 文档清单）

**`文档目录.md` L12-17**（唯一的 markdown 相对链接）：
- `./软件详细设计说明-XX子系统.md` → `./SDD/软件详细设计说明-XX子系统.md`（6 处）

**`AGENTS.md` §2 文档清单表 L29-34**（裸文件名路径列）：
- `` `软件详细设计说明-XX子系统.md` `` → `` `SDD/软件详细设计说明-XX子系统.md` ``（6 处）

### 2. 无需改（纯文本书名号，不含路径）

以下均为 `《软件详细设计说明-XX子系统》` 书名号**纯文本提及**，不含路径信息，迁移后依然正确，**不动**：
- `软件概要设计说明.md`（L290、L501 等）
- 各 SRS 分册（DC/OCC/OM/MC/SWM/COM 的参考文件表）
- `系统页面清单.md`（L46、L71-75）
- 各 SDD 之间的互引（参考文件表）
- `版本变动记录.md` 的历史条目（历史快照，不回改；仅追加新条目）

> 说明：这些是文档「书名」而非「路径」，迁移后书名不变，引用依然成立。

### 3. 追加版本变动记录

**`版本变动记录/版本变动记录.md`** 顶部追加一条（日期 2026-07-17，版本号 `—`，修改文档写「文档目录/AGENTS（路径同步）」，修改内容摘要：6 个 SDD 文档迁入 `SDD/` 子目录，同步更新文档目录与 AGENTS 文档清单的路径引用，文件内容与书名号引用不变）。

## 执行步骤

1. `mkdir SDD`
2. `git mv` 6 个 `.md` 文件到 `SDD/`（保留 git 历史）
3. 编辑 `文档目录.md`：6 行链接加 `SDD/` 前缀
4. 编辑 `AGENTS.md` §2 文档清单表：6 行路径列加 `SDD/` 前缀
5. 在 `版本变动记录/版本变动记录.md` 顶部追加一条记录

## 不在本次范围

- 不新建 SDD 总册（你选择了不加总册的「SDD/」方案）
- 不改书名号纯文本引用（无需改）
- 不碰 xlsx / pen 文件
- 不碰历史版本变动记录条目

## 影响面

低风险纯文档搬迁：6 个文件移动 + 2 个索引文件路径同步 + 1 条变更记录。SDD 内容本身零修改。