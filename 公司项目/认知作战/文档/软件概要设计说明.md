# 软件概要设计说明

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 认知作战平台（v1.0） |
| 文档版本 | V2.0 |
| 密级 | 内部使用 |
| 编制 | 石建国 |
| 编制日期 | 2026-07-07 |

---

## 1 引言

### 1.1 标识

本文件为"认知作战平台"（以下简称"系统"或"平台"）的软件概要设计说明。它是《软件需求规格说明》V2.0 的设计映射，描述系统的总体结构、子系统划分、功能模块、接口、数据、安全与性能设计，作为详细设计与编码实现的依据。

系统由私有化大模型部署（IRS）作为底层推理基础设施，五个业务子系统与一组公共功能构成：数据爬取子系统（DC）、多设备矩阵自动化群控子系统（MC）、运维监控子系统（OM）、蜂群智能体子系统（SWM）、舆情作战业务子系统（OCC），公共功能（COM），以及跨子系统的共享资源域（SR）。

### 1.2 系统概述

平台面向社交媒体，构建"**数据采集 → 分析研判 → 智能体编队 → 编队执行 → 效果评估 → 运维保障**"的完整需求能力闭环。系统按职责分为三层：

- **基础设施层**：私有化大模型部署（IRS）提供本地大模型私有化部署与推理调用，使全系统 AI 推理不依赖外部公有云；数据爬取子系统（DC）负责爬取工程底座与社交媒体数据的采集与分析；运维监控子系统（OM）汇聚全系统日志与指标，完成常态化运维。
- **执行层**：多设备矩阵自动化群控子系统（MC）定位为"设备执行网关与调度引擎"，收口所有落到终端的动作；蜂群智能体子系统（SWM）提供账号人格与作业策略。执行层支持脚本模式与智能体模式两种执行引擎。
- **业务层**：舆情作战业务子系统（OCC）构建"目标识别 → 行动实施 → 效果评估"的作战闭环。公共功能（COM）提供统一认证、权限与组织管理。

账号与内容作为跨子系统共享资源，单列为共享资源域（SR）统一管理，遵循"权威源唯一、使用方引用、变更经事件通知"原则。

### 1.3 文档概述

本文件章节安排如下：第 2 章引用文件；第 3 章软件结构设计，给出总体架构、层次结构、模块划分与数据流/控制流；第 4 章按子系统给出功能模块设计；第 5 章接口概要设计；第 6 章数据概要设计；第 7 章安全设计；第 8 章性能设计；第 9 章需求追踪；第 10 章附录。

需求编号沿用《软件需求规格说明》：功能需求 `FR-XX-NNN`、非功能需求 `NFR-类-N`、外部接口 `EI-NN`、内部接口 `II-NN`、数据 `DR-NN`、约束 `CON-NN`。子系统代号见附录术语表。

本设计遵循 GJB 438C《军用软件开发文档通用要求》，对每个软部件（功能模块）赋予唯一标识编号。模块标识采用 `M-XX-NN`：`M` 表示软部件（Module），`XX` 为子系统代号（DC/MC/OM/COM/SWM/IRS/OCC/SR），`NN` 为模块在该子系统内的两位顺序号，序号与对应功能需求编号末位对齐，以便配置项管理与需求双向追溯。子系统本身作为一级软部件，编号为 `M-XX-00`。

### 1.4 术语和缩略语

沿用《软件需求规格说明》第 1.4 节术语与缩略语，本文件不再重复。关键术语包括：执行网关、执行模式（脚本模式/智能体模式）、控制通道（ADB/无障碍/CDP）、反指纹浏览器、浏览器 Profile、指纹固化、设备风控分级、共享资源、权威源、主数据、扩展数据、事件通知、蜂群、人设标签、抓手、舆情对冲等。

---

## 2 引用文件

| 文件 | 说明 |
| --- | --- |
| 《软件需求规格说明》V2.0 | 本设计的直接输入，提供功能/非功能需求、接口与数据需求 |
| 《多设备矩阵自动化运营系统开发方案 V1.1》 | 设备、远控、账号、内容、任务、脚本等模块的早期设计与阶段规划 |
| 《软件概要设计简述》 | 数据爬取子系统任务驱动采集、监控告警、数据质量的详细设计参考 |

---

## 3 软件结构设计

### 3.1 总体架构

系统采用**分层 + 子系统**架构。底层基础设施向上提供推理、数据与运维能力；中层执行层收口所有设备动作并提供智能体策略；上层业务层指挥作战闭环。所有跨子系统共享的账号与内容资源由共享资源域统一管理，状态变更经事件总线广播。

```mermaid
flowchart TB
    subgraph BIZ["业务层"]
        OCC["舆情作战业务子系统 OCC<br/>目标识别→行动实施→效果评估"]
    end
    subgraph COM_LAYER["公共功能"]
        COM["公共功能 COM<br/>认证/权限/组织"]
    end
    subgraph EXEC["执行层"]
        SWM["蜂群智能体子系统 SWM<br/>账号人格/作业策略"]
        MC["多设备矩阵自动化群控子系统 MC<br/>设备执行网关与调度引擎"]
    end
    subgraph INFRA["基础设施层"]
        DC["数据爬取子系统 DC<br/>采集与分析"]
        OM["运维监控子系统 OM<br/>日志/指标/告警"]
        IRS["私有化大模型部署 IRS<br/>本地大模型推理"]
    end
    SR[("共享资源域 SR<br/>账号/内容<br/>权威源+事件总线")]

    OCC -->|"作战方案/效果回传"| SWM
    SWM -->|"策略下发/作业参数"| MC
    DC -->|"分析数据/情报"| OCC
    MC -.->|"账号/内容主数据执行方"| SR
    SWM -.->|"引用账号/内容"| SR
    OCC -.->|"引用账号/内容/目标"| SR
    IRS -.->|"本地大模型推理"| SWM
    IRS -.->|"本地大模型推理"| OCC
    IRS -.->|"本地大模型推理"| DC
    DC & MC & SWM & OCC & IRS -.->|"日志/指标"| OM
    COM -.->|"认证/鉴权"| OCC & SWM & MC & DC & OM
```

#### 设计原则

系统设计遵循以下源自需求约束（CON-01～CON-11）的核心原则：

1. **动作收口**（CON-07）：所有执行终端动作（无论脚本引擎还是智能体引擎，无论移动端还是桌面端）均经群控执行网关落地，禁止任何子系统或 Agent 绕过，保证可观测、可审计、可熔断。
2. **推理与执行分离**（CON-10）：智能体模式下的视觉推理由 Agent 调用 IRS 本地大模型完成，不经执行网关；推理产出的动作指令再经执行网关落地。
3. **双执行模式**（CON-08）：同时支持脚本模式与智能体模式，共用底层控制通道，均收口于执行网关。
4. **共享资源权威源**：账号/内容主数据由唯一权威源（MC 担任执行方）维护，使用方只引用标识，变更经事件总线通知。
5. **私有化推理**（CON-06）：大模型推理采用私有化部署，不依赖外部公有云。
6. **可观测优先**：各子系统向运维监控子系统上报日志与指标，关键动作全量留痕。

### 3.2 软件层次结构

系统自下而上分为四层，每层只依赖其下层，不反向依赖：

| 层次 | 组成 | 职责 |
| --- | --- | --- |
| 基础设施层 | IRS、DC、OM | 提供私有化推理、数据采集与分析、运维监控底座 |
| 执行层 | MC、SWM | 收口设备动作执行（MC）、提供账号人格与作业策略（SWM） |
| 业务层 | OCC | 作战指挥大脑，目标识别→行动实施→效果评估 |
| 公共与共享 | COM、SR | 跨层认证授权（COM）、跨子系统共享账号/内容（SR） |

```mermaid
flowchart TB
    subgraph L4["公共与共享"]
        COM["COM 认证/权限/组织"]
        SR["SR 共享资源域"]
    end
    subgraph L3["业务层"]
        OCC["OCC 舆情作战"]
    end
    subgraph L2["执行层"]
        SWM["SWM 蜂群智能体"]
        MC["MC 群控执行网关"]
    end
    subgraph L1["基础设施层"]
        DC["DC 数据爬取"]
        OM["OM 运维监控"]
        IRS["IRS 私有化大模型"]
    end
    L3 --> L2 --> L1
    L4 -.-> L1 & L2 & L3
```

### 3.3 模块划分

系统按子系统划分一级软部件，每个子系统内再按功能需求划分二级软部件（功能模块）。一条功能需求可对应一个或多个模块（按职责内聚程度拆分，不硬拆），每个软部件赋予唯一标识编号（`M-XX-NN`），详见第 4 章。软部件清单如下：

| 子系统（一级软部件） | 代号 | 二级软部件（功能模块） |
| --- | --- | --- |
| 数据爬取子系统（M-DC-00） | DC | M-DC-01 任务池与调度、M-DC-02 监控告警、M-DC-03 爬取工程基础设施、M-DC-04 社交媒体数据采集、M-DC-05 数据处理与存储、M-DC-06 数据分析与情报、M-DC-07 情报驱动采集与目标扩散 |
| 多设备矩阵自动化群控子系统（M-MC-00） | MC | M-MC-01 设备管理、M-MC-02 远程控制、M-MC-03 执行网关与双执行模式、M-MC-04 账号维护、M-MC-05 内容管理、M-MC-06 任务管理、M-MC-07 脚本引擎、M-MC-08 可观测性、M-MC-09 环境风控 |
| 账号管理子系统（M-AM-00） | AM | M-AM-01 桌面端 Profile 管理、M-AM-02 桌面端执行网关、M-AM-03 账号注册与验证、M-AM-04 账号生命周期与养护 |
| 运维监控子系统（M-OM-00） | OM | M-OM-01 日志指标汇聚、M-OM-02 告警与运维 |
| 公共功能（M-COM-00） | COM | M-COM-01 用户认证与权限 |
| 蜂群智能体子系统（M-SWM-00） | SWM | M-SWM-01 智能体账号人设、M-SWM-02 任务智能分发、M-SWM-03 行为仿真防伪装、M-SWM-04 作业统计复盘、M-SWM-05 提示词管理、M-SWM-06 记忆管理 |
| 私有化大模型部署（M-IRS-00） | IRS | M-IRS-01 本地大模型部署 |
| 舆情作战业务子系统（M-OCC-00） | OCC | M-OCC-01 目标识别维护、M-OCC-02 行动方案实施、M-OCC-03 效果评估、M-OCC-04 策略库 |
| 共享资源域（M-SR-00） | SR | M-SR-01 账号资源、M-SR-02 内容资源 |

### 3.4 模块间关系

子系统之间存在四类关键协作关系：

**（1）作战指挥链（OCC → SWM → MC）**

舆情作战是全链路指挥大脑：OCC 基于 DC 分析结果与 IRS 推理产出作战方案，经 SWM 调度，最终由 MC 设备矩阵执行落地，效果回传 OCC 评估闭环。

```mermaid
flowchart LR
    DC -->|"分析结果/情报"| OCC
    OCC -->|"作战方案"| SWM
    SWM -->|"策略/作业参数"| MC
    MC -->|"动作日志/执行记录"| SWM
    SWM -->|"效果数据回传"| OCC
```

**（2）动作收口（所有动作 → MC 执行网关）**

无论脚本引擎还是智能体引擎，所有终端动作均经 MC 执行网关鉴权、记录、熔断、转发。智能体视觉推理直接调用 IRS，不经网关；推理产出的动作指令再经网关落地。

```mermaid
flowchart LR
    SCRIPT["脚本引擎"] --> GW["MC 执行网关"]
    AGENT["智能体引擎"] -->|"动作指令"| GW
    AGENT -.->|"视觉推理(不经网关)"| IRS["IRS 本地大模型"]
    GW -->|"鉴权/记录/熔断/转发"| DEV["执行终端<br/>移动端/桌面端"]
    GW -->|"动作日志全量上报"| OM["OM 运维监控"]
```

**（3）共享资源协作（SR：权威源 + 事件通知）**

账号主数据由 MC（担任执行方）维护，DC/SWM/OCC 作为使用方只引用 account_id 并维护各自扩展属性。内容主数据同理。状态变更经事件总线广播，使用方订阅响应。

```mermaid
flowchart LR
    MC[("MC 账号/内容<br/>权威源执行方")] -->|"主数据"| BUS{{"事件总线"}}
    BUS -->|"account.status.changed"| DC & SWM & OCC
    BUS -->|"content.changed"| SWM & OCC
    DC & SWM & OCC -.->|"引用 account_id/content_id<br/>维护扩展属性"| MC
```

**（4）推理支撑（IRS → DC/SWM/OCC）**

IRS 向上为 DC（文本分析）、SWM（行为决策）、OCC（判别与策略生成）提供统一的本地大模型推理能力，不经外部公有云。

**（5）情报驱动反向闭环（DC 分析 → 采集）**

数据爬取子系统内部形成"分析 → 采集"反向驱动闭环：M-DC-06 数据分析与情报产出的高价值目标，经 M-DC-07 情报驱动采集回注 M-DC-01 任务池并按关系扩散采集，新目标回流 M-DC-05 存储与 M-DC-06 分析后可再次触发回注，使 DC 从单向采集管道升级为情报驱动闭环（FR-DC-005、II-16）。该反向闭环区别于 OCC"扩散趋势预判"——本闭环是 DC 侧图扩展式采集，OCC 扩散趋势预判是传播预测分析（本期暂不实现）。

```mermaid
flowchart LR
    DC6["M-DC-06 数据分析与情报"] -->|"情报产出(目标/关联实体/传播节点)"| DC7["M-DC-07 情报驱动采集"]
    DC7 -->|"回注任务(intel_feedback)"| DC1[("M-DC-01 任务池")]
    DC1 --> DC4["M-DC-04 采集"] --> DC5[("M-DC-05 处理存储")]
    DC5 --> DC6
    DC7 -.->|"扩散采集新目标回流"| DC5
```

### 3.5 数据流设计

系统主数据流贯穿"采集→分析→作战→执行→评估"闭环：

```mermaid
flowchart TB
    subgraph 采集["DC 采集流"]
        T1["任务池/调度"] --> T2["爬虫集群"] --> T3["Kafka 总线"]
        T3 --> T4["ETL 清洗去重"] --> T5[("HBase/PG/图库")]
        T5 --> T6["分析与情报"]
        T6 -.->|"情报回注/目标扩散"| T1
    end
    subgraph 作战["OCC 作战流"]
        T6 -->|"情报/分析结果"| O1["目标识别建档"]
        O1 --> O2["行动方案生成"] --> O3["调度蜂群"]
    end
    subgraph 执行["执行流"]
        O3 -->|"作战方案"| S1["SWM 智能分发"]
        S1 -->|"策略/参数"| M1["MC 执行网关"]
        M1 --> M2["终端执行"]
        M2 -->|"动作日志"| M3["MC 可观测性"]
    end
    subgraph 评估["OCC 评估流"]
        M3 -->|"作业执行记录"| O4["效果评估"]
        T6 -->|"干预前后舆情对比"| O4
        O4 -.->|"反馈迭代"| O5[("策略库")]
        O4 -.->|"反馈"| O1
    end
    M3 & T3 -.->|"日志/指标"| OM[("OM 运维数据底座")]
```

关键数据流要点：
- **采集流**：任务驱动，爬虫消费任务池 → Kafka → ETL（双层去重）→ 多存储（HBase/PG/图库）→ 分析；分析产出的情报可经情报回注与目标扩散（FR-DC-005）反向驱动任务池，形成情报驱动闭环。
- **执行流**：作战方案 → SWM 智能分发 → MC 执行网关 → 终端执行 → 动作日志上报。
- **评估流**：MC 作业执行记录 + DC 舆情对比 → OCC 效果评估 → 反馈策略库与目标，形成闭环。
- **运维流**：各子系统日志与指标汇聚至 OM，形成统一运维数据底座。

### 3.6 控制流设计

系统的控制流围绕"执行网关"这一唯一动作收口展开，并区分脚本模式与智能体模式：

```mermaid
sequenceDiagram
    participant U as 调度/指挥
    participant SWM as SWM 蜂群
    participant SE as 脚本引擎
    participant AE as 智能体引擎
    participant IRS as IRS 本地大模型
    participant GW as MC 执行网关
    participant DEV as 执行终端
    participant OM as OM 运维监控

    U->>SWM: 下发作战任务/作业
    alt 脚本模式
        SWM->>SE: 下发作业参数(account/content/时间)
        SE->>GW: 按固定流程产出动作指令
    else 智能体模式
        SWM->>AE: 下发人设/提示词/策略
        AE->>IRS: 视觉推理(截图/上下文) 不经网关
        IRS-->>AE: 下一步动作规划
        AE->>GW: 动作指令
    end
    GW->>GW: 鉴权/记录/熔断判定
    GW->>DEV: 转发动作(ADB/无障碍/CDP)
    DEV-->>GW: 执行结果
    GW->>OM: 动作日志全量上报
    GW-->>SE/AE: 执行结果/熔断通知
```

控制流要点：
- **两种模式共用控制通道**：移动端采用 ADB/无障碍服务，桌面端反指纹浏览器采用 CDP，控制通道对上层透明，执行网关按终端类型路由。
- **网关统一控制**：动作经网关鉴权（校验来源与目标终端合法性）、记录（写入动作日志）、熔断（风控信号或异常频率时停止后续动作）、转发。
- **推理旁路**：智能体视觉推理直接调用 IRS 以保证低延迟，不经网关；只有推理产出的动作指令经网关落地。

---

## 4 功能模块设计

本章按子系统给出功能模块设计。每个模块（二级软部件）标注其软部件标识编号，描述其职责、组成与关键设计。模块与《软件需求规格说明》功能需求的对应关系见第 9 章需求追踪。子系统作为一级软部件（M-XX-00），其下二级软部件编号 M-XX-NN 见各小节。

### 4.1 数据爬取子系统（M-DC-00）

数据爬取子系统（软部件标识 M-DC-00）是基础设施层的数据采集与分析底座，采用"任务驱动采集"范式：采集任务由外部（人工/侦察程序/情报回注）注入任务池，爬虫作为执行器消费任务、按目标定向抓取。子系统对外提供情报与分析能力供 OCC 使用，并通过情报回注与目标扩散（M-DC-07）形成"分析→采集"反向闭环，从单向采集管道升级为情报驱动闭环。

本子系统下文按"模块划分与职责（4.1.1）、模块间关系（4.1.2）、各模块功能描述（4.1.3）"三段组织。

#### 4.1.1 模块划分与职责

数据爬取子系统划分为七个二级软部件（功能模块），对应五条功能需求（FR-DC-001~005）。其中 FR-DC-001 爬取工程基础设施按职责内聚拆为三个模块——任务池与调度、监控告警、爬取工程基础设施，以体现"输入闸门/感知反馈/工程底座"三类不同职责，不硬拆；其余功能需求各对应一个模块。模块划分与职责如下：

| 软部件编号 | 模块名称 | 对应需求 | 职责 |
| --- | --- | --- | --- |
| M-DC-01 | 任务池与调度 | FR-DC-001（拆分） | 系统的输入闸门与调度大脑：消费外部注入的采集任务，按优先级分发爬虫执行，含目标级去重、配额限流、断点续爬、失败重试 |
| M-DC-02 | 监控告警 | FR-DC-001（拆分） | 采集系统的"眼睛"：分钟级感知任务池积压、账号池耗尽、Kafka 积压、爬虫封禁等异常，并反馈调节调度；DC 采集健康度专属，与平台级 OM 分工 |
| M-DC-03 | 爬取工程基础设施 | FR-DC-001（拆分） | 爬取的工程底座：可弹性伸缩的爬虫集群、Kafka 数据流转通道、多存储分库与对接 VPS 代理的代理网络出口 |
| M-DC-04 | 社交媒体数据采集 | FR-DC-002 | 爬虫执行器：消费任务，按任务目标从 TikTok/Instagram/Facebook/YouTube/X 等海外社交平台采集原始数据（用户/内容/互动/关系） |
| M-DC-05 | 数据处理与存储 | FR-DC-003 | 采集系统的"免疫系统"：对原始数据清洗加工并持久化，提供标准 ETL 链路 + 双层去重 + 质量校验 + 全字段血缘 |
| M-DC-06 | 数据分析与情报 | FR-DC-004 | 对加工数据进行分析，提供群体到个体的筛选、关系图谱、情报检索、可视化与文本智能分析，支撑目标识别与内容研判，供 OCC 使用 |
| M-DC-07 | 情报驱动采集与目标扩散 | FR-DC-005 | 反向驱动大脑：打通"分析→采集"反向链路，使情报产出回注任务池并按关系扩散采集，形成情报驱动闭环 |

#### 4.1.2 模块间关系

七个模块以任务池（M-DC-01）为枢纽，构成"任务驱动采集 + 监控反馈调节 + 情报驱动反向闭环"三重协作关系。

```mermaid
flowchart TB
    subgraph DC["数据爬取子系统 M-DC-00"]
        DC1["M-DC-01 任务池与调度"]
        DC2["M-DC-02 监控告警"]
        DC3["M-DC-03 爬取工程基础设施"]
        DC4["M-DC-04 社交媒体数据采集"]
        DC5["M-DC-05 数据处理与存储"]
        DC6["M-DC-06 数据分析与情报"]
        DC7["M-DC-07 情报驱动采集与目标扩散"]
        DC3 -->|"集群/中间件/代理底座"| DC4
        DC1 -->|"下发抓取任务"| DC4
        DC4 -->|"原始数据(Kafka)"| DC5
        DC5 -->|"加工数据"| DC6
        DC6 -->|"情报产出"| DC7
        DC7 -->|"回注任务(intel_feedback)"| DC1
        DC2 -.->|"指标反馈调节"| DC1
        DC5 -.->|"质量/去重指标"| DC2
        DC1 -.->|"任务池水位"| DC2
    end
```

模块间关系分三类：

- **任务驱动采集流（正向数据流）**：M-DC-03 提供集群/中间件/代理底座，M-DC-01 消费任务池下发抓取任务给 M-DC-04；M-DC-04 采集的原始数据经 Kafka 流入 M-DC-05 清洗去重持久化；M-DC-05 加工数据供 M-DC-06 分析产出情报。
- **监控反馈调节（感知反馈）**：M-DC-01 的任务池水位、M-DC-05 的质量与去重指标上报 M-DC-02；M-DC-02 汇聚指标反馈调节 M-DC-01 调度（封禁率升则降速、任务池积压则扩并发），形成自适应系统。
- **情报驱动反向闭环（FR-DC-005）**：M-DC-06 的情报产出（目标个体、关联实体、传播节点）经 M-DC-07 回注 M-DC-01 任务池（携带 source_type=intel_feedback）并按关系扩散采集，新目标回流 M-DC-05 存储与 M-DC-06 分析后可再次触发回注，使 DC 从单向采集管道升级为情报驱动闭环。该反向闭环区别于 OCC"扩散趋势预判"——本闭环是 DC 侧图扩展式采集，OCC 扩散趋势预判是传播预测分析（本期暂不实现）。

#### 4.1.3 各模块功能描述

本节逐一给出七个模块的职责与所含功能的描述。每个模块下按功能列出其能力与关键设计。

##### 4.1.3.1 任务池与调度模块（M-DC-01）

**模块职责**：系统的输入闸门与调度大脑（FR-DC-001 拆分），消费外部注入的采集任务，按优先级分发爬虫执行，含目标级去重、配额限流、断点续爬、失败重试。

本模块含 8 项功能，分述如下。

###### F-DC-01-01 任务注入与多来源接入

【功能编号】F-DC-01-01
【功能名称】任务注入与多来源接入
【所属模块】任务池与调度模块（M-DC-01）
【功能简述】接收人工、侦察程序、战法库/情报回注等多来源注入的采集任务，统一落入任务池，作为系统的输入闸门。
【使用角色】情报分析人员（人工注入）、侦察程序（自动推送）、战法库/情报回注（回调下发）
【前置条件】任务池（task_pool）存储可用；REST API/MQ 接入通道已就绪
【输入】采集任务（人工经 REST API/CSV/Excel 提交、侦察程序经 MQ/Kafka 推送、战法库/情报回注经回调下发）
【处理过程】
  1. 接收各来源的注入请求并解析
  2. 校验任务字段完整性与来源合法性
  3. 按 task_pool 数据结构（task_type/target/strategy/source/priority/status）规范化
  4. 写入任务池，流转至去重流程（F-DC-01-03）
【输出】写入 task_pool 的任务记录
【业务规则/约束】
  - 支持人工（REST API/文件导入）、侦察程序（MQ/Kafka）、情报回注（M-DC-07 回调）多来源注入
  - 统一 task_pool 数据结构
  - 任务注入即进入待去重流程
【异常处理】
  - 注入请求格式非法：拒绝并返回错误
  - 批量注入部分失败：记录失败项支持重试
  - MQ/Kafka 断连：暂存待恢复后补传

###### F-DC-01-02 多任务类型

【功能编号】F-DC-01-02
【功能名称】多任务类型
【所属模块】任务池与调度模块（M-DC-01）
【功能简述】以任务类型承载不同采集意图，使任务可携带目标与策略参数，支撑定向精准采集。
【使用角色】情报分析人员、系统（按类型解析执行）
【前置条件】任务类型枚举已定义
【输入】任务类型标识与对应目标参数
【处理过程】
  1. 读取任务的 task_type 字段
  2. 按类型加载对应的采集执行逻辑
  3. 校验目标参数与类型匹配性
  4. 输出带类型标签的标准化任务
【输出】带类型标签的标准化任务，供调度与采集执行器按类型解析
【业务规则/约束】
  - 六类任务类型：account 定向（指定账号采全/最新帖文）、topic 话题（话题标签/话题词）、keyword 关键词（监控匹配内容）、url_list（一批 URL 定向抓取，最细粒度）、backfill 回溯（指定时间窗口历史补采）、recheck 复查（已采集目标增量更新）
  - 任务类型决定采集执行器的处理逻辑
【异常处理】
  - 未知任务类型：拒绝并告警
  - 目标参数与类型不匹配：提示修正

###### F-DC-01-03 入池目标级去重

【功能编号】F-DC-01-03
【功能名称】入池目标级去重
【所属模块】任务池与调度模块（M-DC-01）
【功能简述】任务入池时对目标规范化并计算指纹去重，消除重复采集任务，从源头节省资源（双层去重第一层）。
【使用角色】系统（自动执行）
【前置条件】目标规范化规则与 target_hash 计算逻辑已配置
【输入】待入池任务的目标参数（URL/账号/话题/关键词/时间范围/采集策略）
【处理过程】
  1. 对目标按类型规范化（URL/账号/话题/时间/策略）
  2. 计算 target_hash（类型+平台+目标+策略）
  3. 查询任务池是否已存在相同 hash
  4. 新任务写入 task_pool（status=pending）；已存在任务合并优先级
【输出】去重判定结果——新任务入池，或已存在任务合并优先级
【业务规则/约束】
  - 规范化规则：URL 统一协议/域名小写/去跟踪参数/排序 query；账号统一平台+uid；话题关键词统一大小写/排序；时间统一 UTC 对齐；策略排序后参与 hash
  - 命中已存在任务则合并优先级取更高值、更新备注，而非重复下发
【异常处理】
  - 规范化规则异常：降级为原始目标入库并告警
  - hash 计算冲突：人工裁定

###### F-DC-01-04 优先级队列调度

【功能编号】F-DC-01-04
【功能名称】优先级队列调度
【所属模块】任务池与调度模块（M-DC-01）
【功能简述】调度中心消费任务池，按优先级权重抢占式分发抓取任务到爬虫 Pod，保证高价值任务优先执行。
【使用角色】系统（调度中心自动执行）
【前置条件】Redis ZSet 优先级队列与 DolphinScheduler 编排可用；爬虫 Pod 在线
【输入】任务池中 status=pending 的任务及其优先级权重
【处理过程】
  1. 轮询/事件触发消费任务池
  2. 按优先级权重排序批量领取（status 改 claimed）
  3. 经配额限流校验（F-DC-01-05）
  4. 经 Kafka 分发到爬虫 Pod
【输出】分发到爬虫 Pod 的抓取任务
【业务规则/约束】
  - 权重参照人工>情报回注>侦察>常规>回溯（对应 P0~P4 五级，权重 100/80/60/40/20）
  - Redis ZSet 优先级队列 + DolphinScheduler 编排 + Kafka 分发
  - 抢占式调度
【异常处理】
  - 调度中心宕机：高可用切换
  - 分发失败：任务回 pending 重排

###### F-DC-01-05 配额限流

【功能编号】F-DC-01-05
【功能名称】配额限流
【所属模块】任务池与调度模块（M-DC-01）
【功能简述】基于账号池/IP 池水位动态调节采集并发，避免资源耗尽触发风控。
【使用角色】系统（自动执行）
【前置条件】账号池/IP 池水位可探测；与监控（M-DC-02）联动通道可用
【输入】账号池/IP 池可用水位、当前消费并发
【处理过程】
  1. 探测账号池/IP 池可用水位
  2. 与当前消费并发比对
  3. 水位充足正常分发，水位不足等待或降并发
  4. 水位变化反馈调节调度
【输出】并发调节指令（扩并发/降并发/等待）
【业务规则/约束】
  - 水位反馈调节调度，与监控（M-DC-02）联动
  - 动态调节消费并发
【异常处理】
  - 水位持续不足：告警并提示补充资源
  - 水位探测失败：降级为保守并发

###### F-DC-01-06 任务状态机与断点续爬

【功能编号】F-DC-01-06
【功能名称】任务状态机与断点续爬
【所属模块】任务池与调度模块（M-DC-01）
【功能简述】驱动任务九态状态流转，支持失败重试与断点续爬，保证任务可恢复、不丢失。
【使用角色】系统（自动执行）
【前置条件】状态机规则与重试策略已配置
【输入】任务执行回报（成功/失败/超时）
【处理过程】
  1. 接收任务执行回报
  2. 按状态机规则流转（pending→claimed→running→done/failed/timeout）
  3. failed/timeout 经 retry（指数退避，≤3 次）回 pending 重排
  4. 超限进 dead 死信；命中内容去重转 skipped
  5. 服务重启后回 pending 任务续跑（断点续爬）
【输出】任务最新状态（pending/claimed/running/done/failed/timeout/retry/dead/skipped）
【业务规则/约束】
  - 九态状态机
  - 指数退避重试≤3 次，超限进死信
  - 断点续爬：任务 status 回 pending，重启续跑
【异常处理】
  - 重试超限：进死信并告警
  - 状态机非法跃迁：拒绝并记录

###### F-DC-01-07 周期复查任务生成

【功能编号】F-DC-01-07
【功能名称】周期复查任务生成
【所属模块】任务池与调度模块（M-DC-01）
【功能简述】对已采集目标周期性自动生成增量复查任务，保持数据新鲜度，而非全量定时巡采。
【使用角色】系统（Cron 自动执行）
【前置条件】Cron 表达式与复查规则已配置；已采集目标清单可读
【输入】Cron 表达式、已采集目标清单
【处理过程】
  1. Cron 触发复查生成
  2. 读取已采集目标清单
  3. 按复查规则生成 recheck 任务
  4. 写入 task_pool
【输出】自动生成的 recheck 复查任务（写入 task_pool）
【业务规则/约束】
  - Cron 仅用于周期性自动生成复查类任务（如已采集账号每日增量复查，采新评论/新互动）
  - 主采集模式始终任务驱动，不存在定时全量巡采
【异常处理】
  - Cron 触发失败：告警并补触发
  - 复查任务生成过多：受配额限流（F-DC-01-05）约束

###### F-DC-01-08 任务审计

【功能编号】F-DC-01-08
【功能名称】任务审计
【所属模块】任务池与调度模块（M-DC-01）
【功能简述】全程留痕任务注入与执行过程，满足认知战情报的可审计性要求。
【使用角色】审计人员（查阅）、系统（自动记录）
【前置条件】审计存储可用
【输入】任务注入与执行过程中的关键事件
【处理过程】
  1. 采集任务注入与执行的关键事件
  2. 记录 source_type/source_ref/remark/injected_at 等字段
  3. 写入审计存储
  4. 与数据血缘（F-DC-05-04）关联形成回溯链
【输出】任务审计记录
【业务规则/约束】
  - 记录谁、何时、为何注入该任务：source_type（来源类型）、source_ref（来源标识，如战法 ID/分析师账号）、remark（备注/采集意图）、injected_at（入池时间）全程留痕
  - 与数据血缘配合，形成任务→数据的完整回溯链
【异常处理】
  - 审计写入失败：暂存重试，不丢审计

**关键设计**：任务驱动调度——外部注入任务池 → 调度中心按优先级消费 → 配额限流 → 分发爬虫 Pod；入池目标级去重为双层去重第一层（第二层见 M-DC-05）；Cron 仅生成复查任务，不存在定时全量巡采。

##### 4.1.3.2 监控告警模块（M-DC-02）

**模块职责**：采集系统的"眼睛"（FR-DC-001 拆分），分钟级感知任务池积压、账号池耗尽、Kafka 积压、爬虫封禁等异常，并反馈调节调度；DC 采集健康度专属，与平台级 OM 分工。

本模块含 5 项功能，分述如下。

###### F-DC-02-01 指标采集（系统+业务）

【功能编号】F-DC-02-01
【功能名称】指标采集（系统+业务）
【所属模块】监控告警模块（M-DC-02）
【功能简述】采集系统与业务运行指标，形成采集健康度的数据底座，支撑告警与反馈调节。
【使用角色】系统（自动采集）
【前置条件】Prometheus 与各 exporter 已部署
【输入】各模块运行状态（节点/Kafka/存储/调度/任务池/采集器）
【处理过程】
  1. 各 exporter 暴露指标端点
  2. Prometheus 按 Pull 模型周期采集
  3. 区分系统指标与业务指标分别入库
  4. 形成时序指标数据
【输出】时序指标数据（写入 Prometheus）
【业务规则/约束】
  - 系统指标+业务指标双覆盖，业务指标优先（认知战场景核心）
  - 系统指标：节点 CPU/内存/磁盘/网络、Kafka Lag/生产速率、存储连接数/慢查询、调度领取延迟
  - 业务指标：任务池水位分布、任务注入/消费速率、各平台采集成功率/封禁率、去重命中率、端到端延迟
【异常处理】
  - 指标采集超时：降级采样
  - exporter 离线：告警

###### F-DC-02-02 告警分级与路由

【功能编号】F-DC-02-02
【功能名称】告警分级与路由
【所属模块】监控告警模块（M-DC-02）
【功能简述】基于指标与规则产生告警，按级别差异化路由通知，保证关键异常及时触达。
【使用角色】运维人员（接收告警）、系统（自动判定与路由）
【前置条件】告警规则与通知渠道已配置；Alertmanager 可用
【输入】告警规则、时序指标
【处理过程】
  1. 评估指标是否命中告警规则
  2. 判定告警级别（致命/严重/警告/提示）
  3. 抑制与聚合防风暴
  4. 按级别路由到对应通知渠道
【输出】告警事件、多渠道通知（电话/IM/邮件/日报）
【业务规则/约束】
  - 四级告警：致命（任务池积压>1万、Kafka 积压>10万、账号池水位<10%、调度宕机，<5 分钟电话+IM+邮件）、严重（某平台成功率<70%、ES 磁盘>85%，<15 分钟 IM+邮件）、警告（单平台成功率<90%、Lag 持续上升，<1 小时 IM）、提示（慢查询增多、Pod 偶发重启，日报汇总）
  - 含告警抑制与聚合防风暴
【异常处理】
  - 告警风暴：按规则聚合去重
  - 通知渠道失效：降级为备用渠道

###### F-DC-02-03 监控大盘

【功能编号】F-DC-02-03
【功能名称】监控大盘
【所属模块】监控告警模块（M-DC-02）
【功能简述】以可视化大盘呈现采集系统运行态势，支撑运维与指挥决策。
【使用角色】运维人员、指挥人员（查看大盘）
【前置条件】Grafana 可用；时序指标数据已入库
【输入】时序指标数据
【处理过程】
  1. 从 Prometheus 读取指标
  2. 按大盘类型聚合渲染
  3. 展示六类大盘
【输出】六类 Grafana 大盘
【业务规则/约束】
  - 六类大盘：总览（任务池水位/全平台成功率/端到端延迟/资源水位）、任务池（入池/消费速率、按来源/类型分布、消费延迟、积压趋势）、平台维度（按 TikTok/X/FB 拆分的成功率与封禁率）、资源池（账号/IP/Cookie 三池水位曲线+预警线）、质量（去重命中率/校验失败率/死信积压）、调度（任务领取延迟/优先级分布/重试率）
【异常处理】
  - 看板数据加载超时：降级展示缓存

###### F-DC-02-04 全链路追踪

【功能编号】F-DC-02-04
【功能名称】全链路追踪
【所属模块】监控告警模块（M-DC-02）
【功能简述】实现任务级全链路 trace，使每条数据从入池到入库可追踪，支撑故障定位与情报回溯。
【使用角色】运维人员、审计人员（查阅 trace）
【前置条件】OpenTelemetry 已接入各环节
【输入】任务在各环节的执行事件
【处理过程】
  1. 各环节上报任务执行事件
  2. OpenTelemetry 串联为任务级 trace
  3. 覆盖入池→调度→抓取→入库全链路
  4. 与任务审计、数据血缘关联
【输出】任务级 trace 链路
【业务规则/约束】
  - 覆盖入池→调度→抓取→入库全链路
  - 与任务审计（F-DC-01-08）、数据血缘（F-DC-05-04）配合，形成完整可追溯链
【异常处理】
  - trace 采集失败：不阻塞主流程，降级为日志补录

###### F-DC-02-05 SLO 与反馈调节

【功能编号】F-DC-02-05
【功能名称】SLO 与反馈调节
【所属模块】监控告警模块（M-DC-02）
【功能简述】定义采集时效与质量目标，并以监控指标反馈调节调度，形成自适应系统。
【使用角色】运维人员（定义 SLO）、系统（自动反馈调节）
【前置条件】SLO 已定义；调度反馈通道可用
【输入】SLO 定义、时序指标
【处理过程】
  1. 计算 SLO 达成情况
  2. 比对指标与反馈阈值
  3. 生成调度反馈调节信号（降速/扩并发）
  4. 历史指标长期存储
【输出】SLO 达成情况、调度反馈调节信号
【业务规则/约束】
  - SLO：任务入池→落地 P99<10min、认知战 SLA<5min
  - 监控指标反馈调节调度——封禁率升则降速、任务池积压则扩并发
  - 历史指标长期存储用于容量规划与复盘
【异常处理】
  - SLO 持续不达标：告警并提示扩容或调优

**关键设计**：本模块聚焦 DC 采集健康度，平台级基础设施运维归 OM（M-OM-01/02）；监控→调度反馈调节形成自适应系统。

##### 4.1.3.3 爬取工程基础设施模块（M-DC-03）

**模块职责**：爬取的工程底座（FR-DC-001 拆分，剥离调度与监控后的底座部分），提供可弹性伸缩的爬虫集群、Kafka 数据流转通道、多存储分库与对接 VPS 代理的代理网络出口。

本模块含 4 项功能，分述如下。

###### F-DC-03-01 爬虫集群

【功能编号】F-DC-03-01
【功能名称】爬虫集群
【所属模块】爬取工程基础设施模块（M-DC-03）
【功能简述】提供可弹性伸缩、可自愈的爬虫执行节点池，承载采集任务的实际抓取。
【使用角色】系统（自动运行）
【前置条件】K8s/K3s 集群可用；Rancher 管理就绪
【输入】调度分发来的抓取任务、集群资源配置
【处理过程】
  1. 接收调度分发的抓取任务
  2. 爬虫 Pod 按任务目标执行抓取
  3. 抓取结果发往 Kafka
  4. 节点故障时自动重启或迁移
【输出】抓取到的原始数据（发往 Kafka）
【业务规则/约束】
  - 爬虫以容器化方式部署于 K8s（K3s/Rancher 管理）
  - 支持横向扩缩容与节点自愈
  - 复用 Kafka/Spark/HBase 等大数据组件（CON-03）
【异常处理】
  - 节点故障：自动重启或迁移
  - 资源不足：按优先级限流并告警

###### F-DC-03-02 Kafka 数据总线

【功能编号】F-DC-03-02
【功能名称】Kafka 数据总线
【所属模块】爬取工程基础设施模块（M-DC-03）
【功能简述】作为采集与处理彻底异步解耦的唯一中枢，承载原始数据的可靠流转。
【使用角色】系统（自动流转）
【前置条件】Kafka 集群可用；分区策略已配置
【输入】爬虫抓取的原始数据
【处理过程】
  1. 接收爬虫写入的原始数据
  2. 按分区策略分区分组
  3. 供下游 ETL 消费
  4. 支持回溯消费
【输出】Kafka 消息流（分区分组、可回溯消费）
【业务规则/约束】
  - 原始数据经 Kafka 分区分组流转，支持回溯消费
  - 作为采集（M-DC-04）与处理（M-DC-05）彻底异步的唯一中枢
【异常处理】
  - Kafka 不可用：数据暂存本地待重试
  - 分区不均衡：自动再均衡

###### F-DC-03-03 代理 IP 池

【功能编号】F-DC-03-03
【功能名称】代理 IP 池
【所属模块】爬取工程基础设施模块（M-DC-03）
【功能简述】提供按平台/地区分配、可轮换、可检测的代理出口，应对海外平台访问限制。
【使用角色】设备运维人员（配置代理）、系统（自动轮换与检测）
【前置条件】VPS 代理链路已对接；平台/地区分配策略已配置
【输入】VPS 代理链路配置、平台/地区分配策略
【处理过程】
  1. 按平台/地区分配代理 IP
  2. 定时轮换代理
  3. 探测代理可用性
  4. 剔除失效代理并补充新代理
【输出】可用代理 IP 列表、轮换日志、可用性状态
【业务规则/约束】
  - 对接 VPS 代理链路，按平台/地区分配代理并定时轮换
  - 进行可用性检测与失效剔除
【异常处理】
  - 代理失效：告警并暂停受影响采集任务、切换备用代理
  - 代理全部失效：告警并暂停采集

###### F-DC-03-04 设备维护服务

【功能编号】F-DC-03-04
【功能名称】设备维护服务
【所属模块】爬取工程基础设施模块（M-DC-03）
【功能简述】维护采集所需的账号/IP/Cookie/设备指纹等资源池，供调度配额限流使用。
【使用角色】系统（自动维护）
【前置条件】各资源池存储可用
【输入】账号、IP、Cookie、设备指纹资源
【处理过程】
  1. 维护账号池/IP 池/Cookie 池/设备指纹资源
  2. 探测各资源可用性
  3. 剔除失效凭据
  4. 暴露资源池水位
【输出】可用的资源池水位与凭据
【业务规则/约束】
  - 维护账号池/IP 池/Cookie 池/设备指纹等资源
  - 水位信息供 M-DC-01（F-DC-01-05 配额限流）使用
【异常处理】
  - 资源耗尽：告警并提示补充
  - 凭据失效：自动剔除并标记

**关键设计**：复用 Kafka/Spark/HBase 等大数据组件（CON-03）；底座与调度（M-DC-01）、监控（M-DC-02）解耦，各司其职。

##### 4.1.3.4 社交媒体数据采集模块（M-DC-04）

**模块职责**：爬虫执行器（FR-DC-002），消费任务，按任务目标从 TikTok/Instagram/Facebook/YouTube/X 等海外社交平台采集原始数据（用户/内容/互动/关系）。

本模块含 4 项功能，分述如下。

###### F-DC-04-01 多平台采集适配

【功能编号】F-DC-04-01
【功能名称】多平台采集适配
【所属模块】社交媒体数据采集模块（M-DC-04）
【功能简述】针对各海外社交平台特性提供适配的采集能力，覆盖多平台多数据类型。
【使用角色】系统（自动采集）
【前置条件】各平台采集适配器已就绪；代理与维护账号可用
【输入】带类型与目标的采集任务、平台标识
【处理过程】
  1. 读取任务平台标识与采集手段
  2. 调用对应平台采集适配器
  3. 按目标抓取用户/内容/互动/关系数据
  4. 输出原始数据
【输出】采集到的原始社交数据（用户信息、内容、互动数据、关系数据）
【业务规则/约束】
  - 各平台采集适配器，按平台采用不同手段——Instagram/TikTok/Facebook/X 采用逆向采集，辅以官方 API、模拟点击、网页解析
  - 覆盖 TikTok/Instagram/Facebook/YouTube/X 等海外社交平台
【异常处理】
  - 逆向接口失效或改版：告警并切换备用采集方式
  - API 限流：按限额退避重试

###### F-DC-04-02 采集手段切换与容错

【功能编号】F-DC-04-02
【功能名称】采集手段切换与容错
【所属模块】社交媒体数据采集模块（M-DC-04）
【功能简述】在采集异常时被动切换手段与资源，保证采集连续性（单次异常的被动应对）。
【使用角色】系统（自动切换）
【前置条件】备用采集方式与备用账号已配置
【输入】采集异常信号（接口失效、限流、账号被封）
【处理过程】
  1. 检测采集异常信号
  2. 判定异常类型（接口失效/限流/账号被封）
  3. 切换到备用采集方式或备用账号
  4. 继续采集任务
【输出】切换后的采集方式与资源
【业务规则/约束】
  - 逆向接口失效或改版时告警并切换备用采集方式
  - API 限流按限额退避重试
  - 账号被封自动切换备用账号
【异常处理】
  - 所有备用手段均失效：任务进 retry（F-DC-01-06）并告警

###### F-DC-04-03 代理与账号应用

【功能编号】F-DC-04-03
【功能名称】代理与账号应用
【所属模块】社交媒体数据采集模块（M-DC-04）
【功能简述】在采集过程中应用代理 IP 与维护账号，应对平台访问限制。
【使用角色】系统（自动应用）
【前置条件】代理 IP 与维护账号可用（来自 F-DC-03-04）
【输入】代理 IP、维护账号
【处理过程】
  1. 从设备维护服务获取代理与账号
  2. 绑定到采集请求
  3. 执行采集
  4. 记录代理与账号使用情况
【输出】绑定代理与账号的采集请求
【业务规则/约束】
  - 采集过程绑定代理 IP、使用维护账号应对平台访问限制
  - 代理与账号选择受 F-DC-04-04 策略优化影响
【异常处理】
  - 代理失效：切换备用代理（F-DC-03-03）
  - 账号失效：切换备用账号

###### F-DC-04-04 采集策略自优化

【功能编号】F-DC-04-04
【功能名称】采集策略自优化
【所属模块】社交媒体数据采集模块（M-DC-04）
【功能简述】基于历史采集指标主动调整采集策略，使策略随平台反爬强度自适应演进，从被动应对升级为主动学习闭环。
【使用角色】系统（自动优化）
【前置条件】历史采集指标可采集；策略优化模型已配置
【输入】历史采集指标（封禁率/成功率/反爬升级信号）、当前采集策略
【处理过程】
  1. 采集历史采集指标
  2. 评估反爬强度变化
  3. 调整手段优先级、请求频率/并发、代理与账号选择
  4. 输出优化后的采集策略
【输出】优化后的采集策略（手段优先级、请求频率/并发、代理与账号选择）
【业务规则/约束】
  - 按封禁率/成功率/反爬升级信号主动调整，使策略随平台反爬强度自适应演进
  - 指标波动过大时维持上一稳定策略并告警，避免抖动
【异常处理】
  - 指标不足或波动过大：不更新策略，维持稳定并告警

**关键设计**：采集策略自优化（F-DC-04-04）区别于被动的手段切换（F-DC-04-02）——前者是"采集→指标→优化采集"的学习闭环，后者是单次异常的被动应对。

##### 4.1.3.5 数据处理与存储模块（M-DC-05）

**模块职责**：采集系统的"免疫系统"（FR-DC-003），对原始数据清洗加工并持久化，提供标准 ETL 链路 + 双层去重 + 质量校验 + 全字段血缘。

本模块含 5 项功能，分述如下。

###### F-DC-05-01 分布式 ETL 加工

【功能编号】F-DC-05-01
【功能名称】分布式 ETL 加工
【所属模块】数据处理与存储模块（M-DC-05）
【功能简述】对原始数据清洗加工，完成字段提取、格式标准化、去重、关联，形成可分析的结构化数据。
【使用角色】系统（自动执行）
【前置条件】Spark/MapReduce 计算集群可用；Kafka 数据可消费
【输入】Kafka 中的原始采集数据
【处理过程】
  1. 从 Kafka 消费原始数据
  2. 执行清洗与字段提取
  3. 格式标准化与去重、关联
  4. 输出结构化数据
【输出】清洗加工后的结构化数据
【业务规则/约束】
  - 基于 Spark/MapReduce 分布式批处理完成清洗、字段提取、格式标准化、去重、关联
  - 复用 Spark/HBase 等大数据组件（CON-03）
【异常处理】
  - 计算节点失败：任务自动重试或转移
  - 数据格式异常：隔离坏数据并记录

###### F-DC-05-02 入库内容指纹去重

【功能编号】F-DC-05-02
【功能名称】入库内容指纹去重
【所属模块】数据处理与存储模块（M-DC-05）
【功能简述】在入库环节按内容指纹去重，解决多任务交叉命中同一条内容（双层去重第二层）。
【使用角色】系统（自动执行）
【前置条件】指纹索引（Redis/HBase）可用
【输入】ETL 加工后的内容数据
【处理过程】
  1. 计算内容指纹（精确/SimHash/语义三层）
  2. 查询指纹索引判断是否重复
  3. 重复内容丢弃并计入去重指标
  4. 不重复内容继续后续校验
【输出】去重判定结果（入库或丢弃）
【业务规则/约束】
  - 三层指纹：精确去重（URL/平台主键，Redis Set/BloomFilter 高频判断）、SimHash（64 位，Hamming 距离≤3 视为重复，仅保留首发）、MinHash+LSH 语义（Jaccard 相似度≥0.85 聚合为同一事件，可选）
【异常处理】
  - 指纹计算失败：降级为精确去重
  - 指纹索引不可用：暂存待重试

###### F-DC-05-03 数据质量校验

【功能编号】F-DC-05-03
【功能名称】数据质量校验
【所属模块】数据处理与存储模块（M-DC-05）
【功能简述】在 ETL 管道内串行校验数据质量，保证脏数据不入主库。
【使用角色】系统（自动校验）、数据分析人员（人工抽检）
【前置条件】校验规则已配置；隔离区（OSS dirty_zone）可用
【输入】去重后的内容数据、校验规则
【处理过程】
  1. 串行执行六类校验
  2. 任一失败进隔离区不入主库
  3. 校验通过进入多存储分发
  4. 每日 0.1% 抽样人工复核校准规则
【输出】校验通过的数据入主库，失败数据进隔离区
【业务规则/约束】
  - 六类校验：Schema（字段完整性/类型/必填项）、时间合理性（发布时间不早于平台成立日、不晚于当前）、内容有效性（正文非空/长度合理/非纯表情乱码）、数值边界（互动数≥0 且不超上限）、平台一致性（账号 ID 格式符合平台规范）、异常检测（单账号高频发布/内容相似度突增/地理语言分布异常）
  - 人工抽检闭环（每日 0.1% 抽样复核）持续校准规则
【异常处理】
  - 校验规则异常：降级为宽松校验并告警
  - 隔离区满：告警

###### F-DC-05-04 数据血缘与可追溯

【功能编号】F-DC-05-04
【功能名称】数据血缘与可追溯
【所属模块】数据处理与存储模块（M-DC-05）
【功能简述】为每条数据附加全字段血缘，满足认知战情报的可审计性，支撑情报争议回溯。
【使用角色】审计人员（回溯查阅）、系统（自动记录）
【前置条件】血缘字段规范已定义
【输入】数据入库过程信息
【处理过程】
  1. 采集数据入库过程的来源信息
  2. 附加全字段血缘
  3. 与任务审计、全链路追踪关联
  4. 写入数据记录
【输出】带全字段血缘的数据记录
【业务规则/约束】
  - 每条数据必带血缘字段：source_task_id（来源任务）、crawl_task_id（采集执行 ID）、crawl_pod（采集节点）、crawl_account_id（使用账号）、crawl_proxy_ip（使用代理）、raw_payload_url（原始报文存档地址）、quality_score（质量评分）、dedup_fingerprint（去重指纹）
  - 凭血缘可完整回溯"谁注入任务→何时调度→哪个节点/账号抓取→如何入库"
【异常处理】
  - 血缘字段缺失：阻断入库并告警
  - 血缘写入失败：暂存重试

###### F-DC-05-05 多存储分发

【功能编号】F-DC-05-05
【功能名称】多存储分发
【所属模块】数据处理与存储模块（M-DC-05）
【功能简述】按数据类型与访问模式分发到对应存储，支撑后续分析与检索。
【使用角色】系统（自动分发）
【前置条件】HBase/PG/MySQL/图数据库/OSS 可写入
【输入】校验通过的加工数据
【处理过程】
  1. 按数据类型判定目标存储
  2. 结构化结果写 HBase
  3. 元数据索引写关系库（PG/MySQL）
  4. 关系数据写图数据库，多媒体/冷数据归档 OSS
【输出】写入各存储的数据
【业务规则/约束】
  - 结构化结果写 HBase（列式），元数据与索引写关系库（PG/MySQL），关系数据写图数据库，多媒体/冷数据归档 OSS
【异常处理】
  - 某存储写入失败：数据进入重试队列，不影响其它存储
  - 存储不可用：降级暂存

**关键设计**：双层去重——入池目标级（M-DC-01，避免重复下发）+ 入库内容指纹级（本模块，解决多任务交叉命中）；脏数据隔离区不污染主库，可复盘；质量指标上报 M-DC-02 监控大盘。

##### 4.1.3.6 数据分析与情报模块（M-DC-06）

**模块职责**：对加工数据进行分析（FR-DC-004），提供从群体到个体的筛选、关系图谱、情报检索、可视化与文本智能分析，支撑目标识别与内容研判，供 OCC 使用，并向 M-DC-07 输出情报驱动反向闭环的情报产出。

本模块含 6 项功能，分述如下。

###### F-DC-06-01 群体到个体筛选

【功能编号】F-DC-06-01
【功能名称】群体到个体筛选
【所属模块】数据分析与情报模块（M-DC-06）
【功能简述】从大范围群体中逐层下钻定位目标个体，支撑目标识别。
【使用角色】情报分析人员（筛选与研判）、系统（辅助筛选）
【前置条件】加工后的数据已就绪；AI 算法已配置
【输入】加工后的数据、群体特征条件
【处理过程】
  1. 读取群体特征条件
  2. 调用 AI 算法逐层下钻筛选
  3. 从群体逐步收敛到目标个体
  4. 输出目标个体集合
【输出】目标个体集合
【业务规则/约束】
  - 结合 AI 算法实现从群体到个体的逐层下钻筛选
  - 从大范围群体中定位目标个体
【异常处理】
  - 算法推理超时或失败：降级返回部分结果并告警

###### F-DC-06-02 关系图谱与图查询

【功能编号】F-DC-06-02
【功能名称】关系图谱与图查询
【所属模块】数据分析与情报模块（M-DC-06）
【功能简述】绘制实体关系图谱并支持图查询，揭示目标间关联网络。
【使用角色】情报分析人员（图谱查询与研判）
【前置条件】关系数据已入图数据库；图数据库可用
【输入】实体与关联数据（图数据库）
【处理过程】
  1. 读取实体与关联数据
  2. 绘制关系图谱
  3. 支持图查询交互
  4. 输出图谱与查询结果
【输出】关系图谱、图查询结果
【业务规则/约束】
  - 基于实体与关联数据绘制关系图谱并支持图查询
【异常处理】
  - 图谱查询数据量过大：分页或限流
  - 图数据库不可用：降级为关系库查询

###### F-DC-06-03 情报检索与实体档案

【功能编号】F-DC-06-03
【功能名称】情报检索与实体档案
【所属模块】数据分析与情报模块（M-DC-06）
【功能简述】多维检索实体并聚合形成全维度档案，支撑情报研判。
【使用角色】情报分析人员（检索与查阅档案）
【前置条件】实体数据已入库
【输入】检索条件（账号/昵称/标识）、实体标识
【处理过程】
  1. 按检索条件查询实体
  2. 输出实体信息列表
  3. 对给定实体输出关联拓扑与情报标签
  4. 聚合多来源形成全维度档案
【输出】实体信息列表、关联拓扑与情报标签、实体全维度档案视图
【业务规则/约束】
  - 提供基础信息检索，按账号/昵称/标识等检索实体信息
  - 对给定实体输出关联拓扑与情报标签，形成关联网络
  - 聚合多来源信息形成实体的全维度档案
【异常处理】
  - 检索无结果：返回空并提示
  - 档案聚合数据不全：标注缺失

###### F-DC-06-04 文本智能分析

【功能编号】F-DC-06-04
【功能名称】文本智能分析
【所属模块】数据分析与情报模块（M-DC-06）
【功能简述】对文本内容进行规则匹配与分类筛选，支撑内容研判与目标识别。
【使用角色】情报分析人员（研判）、系统（自动分析）
【前置条件】文本模型与关键词库已配置
【输入】待分析的文本内容、文本模型、关键词库
【处理过程】
  1. 读取待分析文本
  2. 结合文本模型与关键词库匹配
  3. 执行分类筛选
  4. 输出文本分类与筛选结果
【输出】文本分类与筛选结果
【业务规则/约束】
  - 对文本内容结合文本模型与关键词库进行规则匹配与分类筛选
  - 用于内容研判与目标识别
【异常处理】
  - 文本模型不可用：回退到关键词规则匹配

###### F-DC-06-05 可视化展示

【功能编号】F-DC-06-05
【功能名称】可视化展示
【所属模块】数据分析与情报模块（M-DC-06）
【功能简述】以可视化界面呈现分析结果，支撑情报分析人员研判。
【使用角色】情报分析人员（查看可视化）
【前置条件】分析结果数据可读
【输入】分析结果数据
【处理过程】
  1. 读取分析结果数据
  2. 渲染可视化界面
  3. 支持交互探索
【输出】可视化展示界面
【业务规则/约束】
  - 分析结果可视化展示界面，支撑情报分析人员研判
【异常处理】
  - 可视化渲染失败：降级为表格展示

###### F-DC-06-06 推理容错与降级

【功能编号】F-DC-06-06
【功能名称】推理容错与降级
【所属模块】数据分析与情报模块（M-DC-06）
【功能简述】在算法推理异常时降级保障分析可用性，避免单点失败导致分析中断。
【使用角色】系统（自动降级）
【前置条件】降级策略已配置
【输入】推理异常信号
【处理过程】
  1. 检测推理异常信号
  2. 触发降级策略
  3. 返回部分结果并告警
  4. 与各分析功能的异常处理联动
【输出】降级处理后的部分结果、告警
【业务规则/约束】
  - 算法推理超时或失败时降级返回部分结果并告警
  - 与各分析功能的异常处理联动
【异常处理】
  - 持续推理失败：告警并提示模型/算力问题

**关键设计**：本模块的情报产出（目标个体、关联实体、传播节点）是 M-DC-07 情报驱动采集的输入，构成 DC 内部反向闭环。

##### 4.1.3.7 情报驱动采集与目标扩散模块（M-DC-07）

**模块职责**：反向驱动大脑（FR-DC-005），打通"分析→采集"反向链路，使 M-DC-06 的情报产出回注任务池并按关系扩散采集，形成"发现线索→定向扩散采集→锁定目标"的情报驱动闭环，补全认知作战最关键的反向闭环。

本模块含 2 项功能，分述如下。

###### F-DC-07-01 情报回注采集

【功能编号】F-DC-07-01
【功能名称】情报回注采集
【所属模块】情报驱动采集与目标扩散模块（M-DC-07）
【功能简述】将分析产出的高价值目标回注任务池生成采集任务，打通"分析→采集"反向链路，使 DC 从单向管道升级为情报驱动闭环。
【使用角色】情报分析人员（确认回注）、系统（自动回注）
【前置条件】M-DC-06 分析结果可读；任务池可用；反向接口 II-16 已对接
【输入】M-DC-06 分析产出的高价值目标个体、关联实体、传播节点；回注规则（自动/人工确认、任务类型）
【处理过程】
  1. 读取 M-DC-06 分析产出的高价值目标
  2. 按回注规则生成定向采集任务（account/keyword/url_list）
  3. 高扩散/批量/敏感经人工确认闸门
  4. 携带 source_type=intel_feedback 与来源情报标识回注任务池
  5. 经 F-DC-01-03 目标级去重，命中已采集目标转 recheck
【输出】回注任务池的定向采集任务
【业务规则/约束】
  - 携带 source_type=intel_feedback 与来源情报标识
  - 回注任务经目标级去重，命中已采集目标转为 recheck 复查而非重复采集
  - 高扩散/批量/敏感须经人工确认闸门，防止采集范围失控
  - 全程审计留痕；反向接口 II-16
【异常处理】
  - 回注接口不可用：暂存重试
  - 人工确认超时未响应：按默认策略（不入池）处理并记录

###### F-DC-07-02 目标扩散采集

【功能编号】F-DC-07-02
【功能名称】目标扩散采集
【所属模块】情报驱动采集与目标扩散模块（M-DC-07）
【功能简述】以已采集目标为起点按关系扩散采集，扩展目标网络，形成"发现→扩散采集→锁定"链条。
【使用角色】情报分析人员（配置扩散参数）、系统（自动扩散采集）
【前置条件】关系数据已入图数据库；配额限流（F-DC-01-05）可用
【输入】扩散起点目标、扩散参数（关系类型、扩散层数、单层节点数与总数上限、去重范围）
【处理过程】
  1. 以已采集目标为起点
  2. 按关系类型与扩散层数图扩展式 BFS 采集
  3. 每层新节点经去重后生成新采集任务
  4. 新目标回流 M-DC-05 存储与 M-DC-06 分析
  5. 可再次触发回注（F-DC-07-01）进一步扩散，形成迭代扩散
【输出】扩散采集的新目标与关系数据（回流至 M-DC-05 存储）、扩散采集生成的新任务
【业务规则/约束】
  - 关系类型：关注/粉丝/互动/转发链/评论链
  - 图扩展式 BFS 采集，形成广度优先的目标网络扩展
  - 受配额限流（F-DC-01-05）约束
  - 设层数/单层节点数/总目标数上限，超出自动停止并告警
  - 去重命中率持续过高（无新目标）时停止迭代
【异常处理】
  - 扩散层数或节点数超限：停止并告警
  - 去重命中率持续过高：停止迭代并记录
  - 扩散关系数据缺失：降级为已采集层的部分结果

**关键设计**：本模块的图扩展式采集区别于 OCC 附录中本期暂不实现的"扩散趋势预判"——本模块做 DC 侧目标网络边采全（图扩展爬取），OCC 扩散趋势预判做传播预测分析（预判传播怎么扩散），后者本期不做。


### 4.2 多设备矩阵自动化群控子系统（M-MC-00）

MC（软部件标识 M-MC-00）是执行层的"设备执行网关与调度引擎"，所有设备动作的唯一收口。统一管理移动端（真机/云手机/ARM 板卡/模拟器）与桌面端（反指纹浏览器 Profile）两类执行终端，提供脚本模式与智能体模式两种执行引擎。

```mermaid
flowchart TB
    subgraph MC["群控子系统 M-MC-00"]
        MC1["M-MC-01 设备管理"]
        MC2["M-MC-02 远程控制"]
        MC3["M-MC-03 执行网关与双执行模式"]
        MC4["M-MC-04 账号维护与管理"]
        MC5["M-MC-05 内容管理"]
        MC6["M-MC-06 任务管理"]
        MC7["M-MC-07 脚本引擎"]
        MC8["M-MC-08 可观测性"]
        MC9["M-MC-09 设备环境与风控对抗"]
        MC7 -->|"动作指令"| MC3
        MC6 -->|"调度分发"| MC3
        MC3 --> MC1
        MC8 -.->|"日志上报"| OM2["OM"]
    end
```

#### 4.2.1 设备管理模块（M-MC-01）

- **职责**：统一管理多类型执行终端的接入、分组、状态监控、风控分级与批量操作，提供执行终端资源池。
- **组成**：终端注册（移动端 Agent 长连接注册 / 浏览器 Profile CDP 注册）、设备台账与分组标签、实时状态指标监控（在线离线/电量/CPU/内存/存储/网络）、风控分级（真机与 ARM 板卡=高，云手机与反指纹浏览器=中，模拟器=低）、批量操作（移动端批量安装/重启/截图；浏览器 Profile 批量创建/启动/关闭/截图；对象存储临时链接文件上传下载）。
- **关键设计**：养号等高敏感场景优先选用高真实度终端（CON-09）；终端掉线标记离线并告警；指标异常（过热/存储满/内存超限）时隔离并停止派发任务。

#### 4.2.2 远程控制模块（M-MC-02）

- **职责**：对单台及多台设备提供低延迟实时画面预览与输入控制，支持多设备同屏监控。
- **组成**：WebRTC 画面传输（远程控制通道与任务执行通道分离）、输入事件映射（鼠标键盘 → 触摸/点击/滑动/输入/返回/Home/多任务）、截图录屏采集保存回看、多设备宫格监控（低码率同屏）、剪贴板双向同步。
- **关键设计**：WebRTC SFU 单节点并发多路；单设备端到端延迟 ≤ 300ms（NFR-P-01）；断流自动重连或降级；弱网自动降帧。

#### 4.2.3 执行网关与双执行模式模块（M-MC-03）⭐ 核心

- **职责**：作为所有执行终端动作的唯一收口，提供脚本模式与智能体模式两种执行引擎，覆盖移动端与桌面端，支撑群控、养号、内容投放三类执行目标。
- **组成**：执行网关（鉴权、记录、熔断、转发）、脚本模式引擎入口、智能体模式引擎入口、控制通道路由（移动端 ADB/无障碍，桌面端 CDP）。
- **关键设计**：
  - **动作收口**（CON-07）：所有动作经网关鉴权（校验来源与目标终端合法性）、记录（写入动作日志并全量上报 OM）、熔断（风控信号或异常频率时停止后续）、转发；
  - **推理执行分离**（CON-10）：智能体视觉推理由 Agent 调用 IRS，不经网关；动作指令再经网关落地；
  - **双模式共用通道**：两种模式按终端类型适配底层控制通道，通道对上层透明；
  - 异常：网关过载按优先级限流；智能体推理失败降级为脚本模式或人工介入；触发风控熔断告警；终端掉线挂起任务待恢复续接；CDP 断开重连。

#### 4.2.4 账号维护与管理模块（M-MC-04）

- **职责**：承担账号资源域（SR）的执行方职责，管理账号注册、验证、日常维护、风险评估与生命周期，维护群控侧扩展属性（设备绑定、登录状态、操作记录）。
- **组成**：凭据加密存储、账号验证（登录探测 + 状态识别 + 风险等级输出）、日常维护（拟人化节奏模拟发帖/关注/浏览）、多渠道注册（邮箱/手机接码/虚拟号）、生命周期状态机（待登录/正常/受限/需验证/封禁/归档）、账号设备绑定、登录状态与操作记录。
- **关键设计**：账号主数据由本模块作为 SR 执行方维护（凭据加密不落明文，NFR-S-03）；状态变更经事件总线广播 `account.status.changed`，使用方订阅响应；养号拟人化策略由 SWM 提供，动作执行收口于执行网关。

#### 4.2.5 内容管理模块（M-MC-05）

- **职责**：承担内容资源域（SR）权威源执行方职责，管理素材入库、分类、审核与发布排期。
- **组成**：素材分类管理（标签/平台分类/账号适配）、定时发布排期、发布前审核与敏感词检测。
- **关键设计**：内容主数据由本模块作为 SR 权威源执行方维护；内容新增或状态变更经事件总线广播 `content.changed`；素材上传失败断点续传；敏感词命中拦截待人工复核。

#### 4.2.6 任务管理模块（M-MC-06）

- **职责**：把操作意图转换为设备可执行任务并调度，支撑排队、分发、重试与定时执行。
- **组成**：任务实例生成与消息队列入队、调度器分发到设备 Agent、八类任务类型（App 安装启动/文件分发/内容发布/截图/数据采集/自动化脚本/设备巡检/账号状态检查）、任务状态机（待执行/排队中/执行中/成功/失败/已取消/超时/部分成功）、失败重试与超时配置、计划任务（一次性/每日/每周/自定义 Cron + 最大并发 + 优先级）。
- **关键设计**：任务从入队到分发 ≤ 2 秒（NFR-P-04）；支持任务优先级与最大并发控制；队列积压按优先级调度并告警。

#### 4.2.7 脚本引擎模块（M-MC-07）

- **职责**：提供脚本模式执行引擎，覆盖模板任务→可视化工作流→脚本开发的递进式自动化能力，产出动作指令经执行网关落地。
- **组成**：模板化任务（打开 App/等待/点击/输入/上传/截图/返回/滑动/关闭/巡检/状态检测）、可视化工作流编排（开始/设备选择/启动 App/等待/点击/输入/滑动/截图/上传/条件/循环/HTTP/日志/审批/结束）、脚本开发与调试（编辑/调试设备/执行日志/截图回放/版本管理）、平台模拟点击自动化（TikTok/Facebook 等）。
- **关键设计**：脚本运行于沙箱并配置权限白名单（NFR-S-05），越权访问敏感路径时拦截；脚本动作均经执行网关落地，不直接操作设备；调试设备掉线暂停调试。

#### 4.2.8 可观测性模块（M-MC-08）

- **职责**：使设备、账号、任务的每一个动作可知、可追溯，支撑记录、回传、告警与审计。
- **组成**：动作留痕（记录时间/设备/账号/结果）、任务执行日志与关键步骤截图回传、异常告警（任务失败/设备掉线/账号异常）、审计日志（谁在何时对哪台设备或账号做了什么）。
- **关键设计**：执行网关的动作日志与熔断记录一并留痕并全量上报 OM（经 II-12）；日志上报失败本地暂存待重试；审计日志写入失败时阻塞相关操作保证不丢审计。本模块是 SWM 作业统计（FR-SWM-004）与 OCC 效果评估（FR-OCC-003）的原始数据来源。

#### 4.2.9 设备环境与风控对抗模块（M-MC-09）

- **职责**：管理移动端与桌面端两类执行终端环境并应对平台风控，通过指纹管理、风控识别、代理分配与环境隔离降低被处置风险。
- **组成**：移动端指纹管理（真机/云手机/板卡硬件与软件指纹）、桌面端浏览器指纹管理（反指纹浏览器 Profile 渲染层 20+ 维度指纹，基于 BotBrowser 定制内核源码级伪装）、指纹固化（Profile 创建时生成并固定）、风控信号识别与规避、独立代理 IP 分配、终端环境相互隔离（指纹/网络/账号/Cookie/缓存；浏览器 Profile 间 Cookie/LocalStorage/缓存独立）。
- **关键设计**：反指纹浏览器基于开源 BotBrowser 定制，采用 Chromium 源码级指纹伪装，控制通道采用 CDP（CON-11）；一个 Profile 对应一套固定指纹与一个账号身份；代理失效切换备用代理。

### 4.3 运维监控子系统（M-OM-00）

OM（软部件标识 M-OM-00）是基础设施层的运维数据底座，汇聚全系统日志与指标，支撑常态化运维。

```mermaid
flowchart LR
    subgraph SRC["各子系统"]
        DC & MC & SWM & OCC & IRS
    end
    SRC -->|"日志/指标上报 II-04/II-12"| OM
    subgraph OM["运维监控子系统 M-OM-00"]
        OM1["M-OM-01 日志与指标汇聚"]
        OM2["M-OM-02 告警与运维"]
        OM1 --> OM2
    end
    OM1 --> LOG[("统一日志库 ES/Loki")]
    OM1 --> TS[("时序库 Prometheus")]
```

#### 4.3.1 日志与指标汇聚模块（M-OM-01）

- **职责**：汇聚 DC、MC 及平台自身日志与指标，形成统一运维数据底座。
- **组成**：日志采集（汇聚统一日志库，支持全文检索）、指标采集（设备在线率/任务成功率/爬虫吞吐量/队列积压等时序数据）。
- **关键设计**：日志采集 Agent 离线本地暂存待恢复上报；指标采集超时降级采样；近 7 日日志检索 ≤ 3 秒（NFR-P-06）。

#### 4.3.2 告警与运维模块（M-OM-02）

- **职责**：基于指标与规则产生告警，通过仪表盘与运维操作支持常态化运维。
- **组成**：阈值告警与异常检测告警（多渠道通知）、多维可视化看板（总览/设备/任务/爬虫/异常）、常态化运维操作（日志查询/故障定位/巡检/容量观测）。
- **关键设计**：告警风暴时按规则聚合去重；通知渠道失效降级备用渠道；看板加载超时降级展示缓存。

### 4.4 公共功能（M-COM-00）

COM（软部件标识 M-COM-00）为各子系统提供统一的认证、权限与组织管理。

#### 4.4.1 用户认证与权限模块（M-COM-01）

- **职责**：提供统一用户认证、基于角色的访问控制（RBAC）与多组织数据隔离。
- **组成**：登录认证（支持双因素）、RBAC 权限决策（超级管理员/运营主管/运营人员/脚本开发者/审核人员/只读观察员等角色）、多组织/多租户数据隔离。
- **关键设计**：登录密码错误超限锁定账号；双因素验证失败拒绝并记录；越权访问拦截并审计；禁止跨组织访问（NFR-C-01）。

### 4.5 蜂群智能体子系统（M-SWM-00）

SWM（软部件标识 M-SWM-00）是执行层的"智能来源"，为设备作业提供账号人格与作业策略。本子系统**不直接操作设备**，所有设备动作收口于 MC 执行网关。产出按执行模式不同：智能体模式下向 Agent 下发人设/提示词/策略（Agent 调用 IRS 自主决策，动作再经网关落地）；脚本模式下向脚本引擎下发作业参数。

```mermaid
flowchart TB
    subgraph SWM["蜂群智能体子系统 M-SWM-00"]
        SWM1["M-SWM-01 智能体账号人设"]
        SWM2["M-SWM-02 任务智能分发"]
        SWM3["M-SWM-03 行为仿真防伪装"]
        SWM4["M-SWM-04 作业统计复盘"]
        SWM5["M-SWM-05 提示词管理"]
        SWM6["M-SWM-06 记忆管理"]
    end
    SWM1 & SWM5 & SWM6 --> SWM2
    SWM3 --> SWM2
    SWM2 -->|"策略/参数下发"| MC3["MC 执行网关"]
    MC8["MC 可观测性"] -.->|"作业执行记录"| SWM4
    SWM4 -.->|"效果回传"| OCC
```

#### 4.5.1 智能体账号分层分组与人设管理模块（M-SWM-01）

- **职责**：作为账号资源域（SR）使用方，对智能体账号分层分组、绑定人设标签、管理作业生命周期。不维护账号主数据，仅引用 account_id，维护蜂群侧扩展属性。
- **组成**：多维分层分组（业务目标/平台/地区/风险等级）、人设标签绑定（身份特征/立场倾向/语言风格/兴趣领域）、作业生命周期状态机（待激活/活跃/休眠/受限/封禁/回收）、状态事件订阅。
- **关键设计**：只引用 account_id 不复制主数据；订阅 `account.status.changed` 事件，收到封禁/受限立即休眠智能体并停止作业；账号服务接口不可用降级本地缓存只读。

#### 4.5.2 蜂群任务智能分发模块（M-SWM-02）

- **职责**：将作战任务智能分发给蜂群智能体账号，支持分时投送与碎片化仿真作业调度，降低批量特征。
- **组成**：智能匹配（任务目标 + 人设标签）、分时投送（按时间窗口分散下发）、碎片化作业拆解（离散动作 + 拟人化随机节奏分批调度）、智能体级作业迁移与续接。
- **关键设计**：任务拆解为碎片化离散动作以拟人化节奏执行，形成仿真作业轨迹降低批量特征；智能体所在终端离线时迁移作业到该智能体的其他可用终端，或恢复后续接；设备级负载均衡与容错重传由 MC 承担，本模块只负责智能体级调度决策。

#### 4.5.3 智能体行为仿真与防伪装模块（M-SWM-03）

- **职责**：实现智能体账号与社交账号联动，模拟随机行为并对批量特征伪装，降低被风控识别概率。
- **组成**：智能体-账号联动机制（行为与人设标签一致）、随机真人行为注入（浏览/停留/互动）、防批量特征伪装（时间间隔/操作频率/内容措辞/设备特征维度打散同质化）、时段智能适配（主动择时到低风险时段）。
- **关键设计**：策略层择时由本模块承担，设备级频次上限与冷却由 MC 任务管理承担，风控信号识别与规避由 MC 风控对抗承担；仿真行为触发平台验证时暂停告警。

#### 4.5.4 蜂群作业统计与复盘模块（M-SWM-04）

- **职责**：对蜂群作业进行数据统计、任务复盘，并将投送效果联动回传上游供效果评估闭环。**不重复采集动作数据**，原始数据来源于 MC 可观测性（FR-MC-008）上报的动作日志与任务日志。
- **组成**：多维统计（任务完成率/内容触达/互动表现/账号健康度）、任务复盘（失败任务/异常账号/低效作业归因）、效果回调回传 OCC。
- **关键设计**：从蜂群作业视角统计 MC 上报的原始动作数据（视角不同而非重复采集）；MC 数据源缺失时标注统计不完整；回调上游失败暂存重试。

#### 4.5.5 蜂群提示词管理模块（M-SWM-05）

- **职责**：管理驱动智能体作业的提示词，支持模板库分类、版本管理、场景适配、动态调用与迭代优化。
- **组成**：提示词模板库（按用途/平台/目标分类）、版本管理（对比/回滚/变更记录）、场景适配（按人设/受众/态势选模板）、动态调用填充（生成最终作业指令）、迭代优化（结合效果反馈淘汰低效模板）。
- **关键设计**：模板填充失败降级默认模板；版本回滚失败告警；效果反馈缺失暂停迭代。

#### 4.5.6 蜂群记忆管理模块（M-SWM-06）

- **职责**：实现智能体作业记忆的存储、场景沉淀、历史复用与个性化状态延续。
- **组成**：记忆存储（已完成任务/互动对象/立场表达等关键信息）、场景沉淀（重复场景经验形成可复用行为模式）、历史复用、个性化状态延续（长期行为符合人设且不断演进）。
- **关键设计**：记忆存储失败暂存待重试；记忆冲突以最新为准；复用记忆偏离人设时纠正。

### 4.6 私有化大模型部署（M-IRS-00）

IRS（软部件标识 M-IRS-00）是系统底层推理基础设施（非业务子系统），部署本地大模型并提供推理调用，使全系统 AI 推理不依赖外部公有云。

#### 4.6.1 本地大模型部署模块（M-IRS-01）

- **职责**：在私有化环境中部署本地大模型，构建底座推理能力，对外提供统一推理接口供 DC/SWM/OCC 调用。
- **组成**：模型加载与服务实例化、统一推理调用接口（对外 EI-06 / II-08）。
- **关键设计**：部署于配备 GPU 算力的私有化服务器或算力集群，不依赖外部公有云模型服务（CON-06）；模型加载失败回退上一可用版本；服务实例化失败告警重试；算力资源不足拒绝新请求并排队。

### 4.7 舆情作战业务子系统（M-OCC-00）

OCC（软部件标识 M-OCC-00）是全链路指挥大脑，构建"目标识别 → 行动实施 → 效果评估"作战闭环。基于 DC 分析结果与 IRS 推理能力，对舆情目标识别维护，产出作战方案并调度 SWM 执行落地，最终评估效果迭代。本期实现目标识别（含维护）、行动方案实施、效果评估与策略库四项能力。

```mermaid
flowchart LR
    DC -.->|"分析数据/情报 II-14"| OCC
    subgraph OCC["舆情作战业务子系统 M-OCC-00"]
        O1["M-OCC-01 目标识别与维护"]
        O2["M-OCC-02 行动方案实施"]
        O3["M-OCC-03 效果评估"]
        O4["M-OCC-04 舆论作业策略库"]
        O1 --> O2
        O2 -->|"调度蜂群"| SWM2["SWM"]
        SWM2 -.->|"效果回传"| O3
        MC8["MC 可观测性"] -.->|"作业执行记录"| O3
        DC -.->|"干预前后舆情"| O3
        O3 -.->|"反馈迭代"| O4
        O4 -.->|"策略"| O2
    end
```

#### 4.7.1 目标识别与维护模块（M-OCC-01）

- **职责**：识别舆情事件目标要素并全生命周期维护——目标即数据对象，支持建档、修改、关联、状态流转与删除，非一次识别即定型的只读结果。融合情报侦察、多维画像与抓手挖掘中"识别目标"的部分。
- **组成**：目标建档（赋予全局唯一 target_id）、目标属性维护（事件主体/传播源头/核心争议点/干预目标/传播线索/多维画像/干预切入点与薄弱点，可增删改）、目标关联（目标间建立关联关系形成关系网络）、目标状态生命周期（待核实/已确认/已锁定/已处置/已归档）、目标变更事件广播。
- **关键设计**：本期以人工识别为主、系统提供数据维护支撑（识别判断由情报分析人员录入，系统负责存储/关联/状态管理/变更通知）；目标变更经事件总线广播 `target.changed`，使用方（行动方案实施、效果评估）订阅响应；后续逐步接入自动识别与画像建模。

#### 4.7.2 行动方案实施模块（M-OCC-02）

- **职责**：根据目标档案与干预切入点匹配干预策略、生成作业方案并调度蜂群执行落地，形成"目标→方案→执行"闭环。
- **组成**：干预策略匹配（根据目标档案中干预切入点）、作业方案生成（含内容/账号/时间/节奏）、调度执行（调度至 SWM 执行落地，由 MC 设备矩阵实施）。
- **关键设计**：本期干预切入点与策略匹配以人工为主，系统提供方案生成与调度支撑；策略匹配失败降级人工选择；蜂群资源不足排队或缩减规模；执行落地失败回滚告警。

#### 4.7.3 效果评估模块（M-OCC-03）

- **职责**：完成单轮干预成效统计、舆论走势修正、策略迭代反馈与作业复盘，支持中短期与长期效果评估。**不重复采集原始数据**——舆情数据来源于 DC、作业执行数据来源于 MC 可观测性（FR-MC-008）。
- **组成**：单轮成效统计（基于 DC 舆情数据 + MC 作业数据，触达/互动/立场变化）、舆论走势修正评估（基于 DC 前后对比）、策略迭代反馈（反馈策略库）、作业复盘（结合 MC 作业执行记录）、中短期实网绩效与长期舆论塑造效果评估接口。
- **关键设计**：双数据源（DC 舆情 + MC 作业执行记录），OCC 基于此分析不自行采集；DC/MC 数据源缺失标注评估不完整；走势修正偏差大提示人工复核；反馈写入策略库失败暂存重试。

#### 4.7.4 舆论作业策略库模块（M-OCC-04）

- **职责**：搭建并迭代多语种、多场景、多风格的舆论作业策略库，为作战提供可复用、可演进的策略资产。
- **组成**：多语种多场景多风格策略组织、分类管理与版本控制、效果反馈迭代优化、场景适配与动态调用。
- **关键设计**：策略冲突人工裁定；版本回滚失败告警；迭代反馈不足暂停该策略迭代。

### 4.8 共享资源域（M-SR-00）

账号与内容是跨子系统的平台级共享资源，不归属任何单一子系统，单列为 SR（软部件标识 M-SR-00）统一描述。遵循"权威源唯一、使用方引用、变更经事件通知"原则。

```mermaid
flowchart LR
    subgraph SR["共享资源域 M-SR-00"]
        SR1["M-SR-01 账号资源<br/>权威源执行方: MC"]
        SR2["M-SR-02 内容资源<br/>权威源执行方: MC"]
    end
    MC[("MC 主数据")] --- SR1 & SR2
    SR1 -.->|"account_id 引用"| DC & SWM & OCC
    SR2 -.->|"content_id 引用"| SWM & OCC
    SR1 -->|"account.status.changed"| BUS{{"事件总线"}}
    SR2 -->|"content.changed"| BUS
    BUS --> DC & SWM & OCC
```

#### 4.8.1 账号资源模块（M-SR-01）

- **职责**：账号资源共享资源域。MC 担任账号主数据执行方，统一管理注册、凭据、验证、风险评估与生命周期；DC/SWM/OCC 作为使用方仅引用 account_id 并维护各自扩展属性。
- **权威源与使用方**：主数据（凭据/平台/全局状态/生命周期/风险评估，凭据加密）由 MC 唯一维护；account_id 全局唯一引用，贯穿 DC（采集关联）/SWM（人设标签）/OCC（目标标注）。
- **事件机制**：账号状态变更（封禁/受限）经事件总线广播 `account.status.changed`，使用方订阅后立即响应（停止任务/休眠智能体/移除目标），保证被封账号全网即时失效。

#### 4.8.2 内容资源模块（M-SR-02）

- **职责**：内容资源共享资源域。MC 担任内容主数据唯一权威源，统一管理素材入库、分类、审核与发布排期；SWM（养号文案）/OCC（对冲素材）/未来 AI 素材制作为使用方，引用 content_id 获取内容并维护各自平台适配版本与使用记录。
- **权威源与使用方**：主数据（原始素材/分类/审核状态/来源）由 MC 唯一维护；content_id 全局唯一引用，贯穿 SWM/OCC。
- **事件机制**：内容新增或状态变更经事件总线广播 `content.changed`，使用方订阅响应（刷新缓存/更新平台适配）。

---

## 5 接口概要设计

所有外部接口使用鉴权与传输加密。接口编号对应《软件需求规格说明》。

### 5.1 外部接口

| 编号 | 接口 | 对接对象 | 输入 | 输出 |
| --- | --- | --- | --- | --- |
| EI-01 | 社交平台交互 | TikTok/Instagram/Facebook/YouTube/X | 采集请求、投放内容、账号操作指令 | 原始社交数据、投放/操作执行结果 |
| EI-02 | 代理 IP 服务 | VPS 代理链路与代理 IP 供应商 | 代理分配请求（平台/地区/数量） | 可用代理 IP 列表、可用性状态、轮换日志 |
| EI-03 | 接码与虚拟号服务 | 手机号接码与虚拟号服务 | 注册请求、验证码获取请求 | 验证码、注册结果、虚拟号凭据 |
| EI-05 | 对象存储 | MinIO 或阿里云 OSS | 上传文件、下载请求、签名 URL 请求 | 文件存储路径、限时签名下载链接、存储结果 |
| EI-06 | 本地大模型推理 | 底层 IRS | 推理请求（文本/截图、任务类型、上下文、人设参数） | 推理结果（分类标签、立场、情感、观点、规划动作） |

### 5.2 内部接口

| 编号 | 接口 | 提供方 → 调用方 | 说明 |
| --- | --- | --- | --- |
| II-01 | 账号服务 | SR（MC 执行方）→ DC/SWM/OCC | 账号查询/变更，返回主数据 |
| II-01a | 账号状态变更事件 | SR（MC）→ 事件总线 → DC/SWM/OCC | account.status.changed，使用方订阅响应 |
| II-02 | Agent 指令与日志 | MC 服务端 ↔ 设备端 Agent | 下发指令、回传日志与截图 |
| II-03 | 远控画面传输 | MC 设备 → WebRTC SFU → 操作端 | 实时画面流（低延迟）、输入事件 |
| II-04 | 日志与指标上报 | 各子系统 → OM | 子系统日志、运行指标上报 |
| II-05 | Kafka 数据总线 | DC 内部采集 → 处理存储 | 原始采集数据、共享资源状态变更事件 |
| II-06 | 管理 REST | 各子系统 → 前端与外部 | 设备/任务/内容/账号/审计 CRUD |
| II-07 | 执行网关动作 | MC 执行网关 ← 脚本引擎/智能体引擎 | 动作指令 → 执行结果、动作日志、熔断记录 |
| II-08 | 本地大模型推理调用 | 设备端 Agent → IRS | 推理请求 → 视觉理解、动作规划；**不经执行网关** |
| II-09 | 策略下发 | SWM → 设备端 Agent | 人设标签、提示词、作业策略 |
| II-10 | 脚本作业参数 | SWM → 脚本引擎 | 作业参数（account_id/content_id/时间窗口/任务类型） |
| II-11 | 作战方案与效果回传 | OCC ↔ SWM | 下发作战方案、回传作业执行效果数据 |
| II-12 | 执行网关日志上报 | MC 执行网关 → OM | 全量动作日志、熔断记录 |
| II-13 | 内容服务 | SR（MC 执行方）→ SWM/OCC | 内容查询、素材获取 |
| II-13a | 内容变更事件 | SR（MC）→ 事件总线 → SWM/OCC | content.changed，使用方订阅响应 |
| II-14 | 分析数据与情报 | DC → OCC | 舆情数据、传播数据、文本分析、关系图谱 |
| II-15 | 桌面端执行网关动作 | AM 执行网关 ← 脚本引擎 | 浏览器 Profile 动作指令 → 执行结果、动作日志、熔断记录；与 II-07 移动端网关并列 |
| II-16 | 情报回注采集 | DC 分析（M-DC-06）→ DC 任务池（M-DC-01） | 情报产出（目标/关联实体/传播节点）回注生成采集任务（source_type=intel_feedback）；与 II-14 方向相反，构成 DC 内部"分析→采集"反向闭环（FR-DC-005） |

### 5.3 接口约束

1. **执行网关收口**（CON-07）：所有执行终端动作必须经 II-07（执行网关动作接口）落地，禁止任何子系统或 Agent 绕过。
2. **推理旁路**（CON-10）：智能体视觉推理经 II-08 直接调用 IRS，不经执行网关，保证低延迟；推理产出的动作指令再经 II-07 落地。
3. **共享资源引用**：使用方只引用资源标识（account_id/content_id），不复制主数据；主数据由 SR 统一管理（MC 担任执行方）；状态变更一律经事件总线（II-01a/II-13a）通知。
4. **鉴权与加密**：所有接口使用鉴权与传输加密（HTTPS/TLS），远程控制通道加密（NFR-S-02）；文件访问使用签名 URL 限时有效（NFR-S-07）。

---

## 6 数据概要设计

### 6.1 数据总体组织

系统数据按权威源模式分层存储。账号与内容作为共享资源，主数据只存于权威源（MC 担任执行方）且唯一一份；使用方只存"资源标识 + 自有扩展属性"，不复制主数据；资源标识（account_id/content_id）在全系统唯一；主数据状态变更经事件总线广播。

### 6.2 数据清单

| 编号 | 数据类别 | 归属 | 存储说明 |
| --- | --- | --- | --- |
| DR-01 | 设备数据 | MC | 设备、设备分组、设备指标 |
| DR-02M | 账号主数据 | SR（MC 执行方） | 凭据（加密）、平台、状态、生命周期、风险评估 |
| DR-02E2 | 账号扩展数据 | DC/SWM/OCC | 采集关联（DC）、人设标签与作业生命周期（SWM，见 DR-09）、目标标注（OCC） |
| DR-03M | 内容主数据 | SR（MC 执行方） | 原始素材、分类、审核状态、来源 |
| DR-03E | 内容扩展数据 | SWM/OCC | 平台适配版本、使用记录 |
| DR-04 | 任务数据 | MC | 任务、任务实例、任务日志 |
| DR-05 | 脚本与工作流数据 | MC | 脚本、脚本版本、工作流定义 |
| DR-06 | 采集与分析数据 | DC | ETL 结果存 HBase，关系数据存图数据库 |
| DR-07 | 审计与风险数据 | COM/MC | 审计日志、风险事件 |
| DR-08 | 代理与指纹数据 | MC | 代理 IP 池、设备指纹配置 |
| DR-09 | 蜂群智能体数据 | SWM | 人设标签、分层分组、作业生命周期、作业记忆、提示词模板版本 |
| DR-10 | 本地大模型数据 | IRS | 本地大模型配置、推理调用日志 |
| DR-11 | 舆情作战数据 | OCC | 目标识别结果、传播链路、多维画像、抓手挖掘结果、作战方案、策略库版本、效果评估记录 |

### 6.3 数据存储选型

| 存储组件 | 用途 |
| --- | --- |
| PostgreSQL / MySQL | 元数据、索引、关系型业务数据（设备/任务/账号/内容主数据等） |
| Redis | 缓存与键值、任务去重、调度队列、分布式锁 |
| Kafka | 消息中枢（DC 采集数据流转、事件总线） |
| HBase | 列式存储，ETL 结果、大规模采集数据 |
| 图数据库 | 关系数据、关系图谱、关联拓扑 |
| MinIO / OSS | 对象存储，多媒体素材、截图、录屏、冷数据归档 |
| Elasticsearch / Loki | 日志全文检索 |
| Prometheus + Grafana | 时序指标与可视化 |

### 6.4 共享资源数据约束

- 主数据只存于权威源（MC 担任执行方）且唯一一份；
- 使用方只存"资源标识 + 自有扩展属性"，不复制主数据；
- account_id 与 content_id 在全系统唯一，贯穿各子系统；
- 主数据状态变更经事件总线广播，使用方订阅响应保证全网一致。

---

## 7 安全设计

依据《软件需求规格说明》安全性与保密性需求设计。

### 7.1 安全性设计（NFR-S）

| 编号 | 需求 | 设计措施 |
| --- | --- | --- |
| NFR-S-01 | 用户认证与双因素，全部 API 鉴权 | COM 统一认证服务，颁发令牌；网关层对所有 API 鉴权 |
| NFR-S-02 | 全程 HTTPS/TLS，远控通道加密 | 全站 TLS；WebRTC 远控通道加密传输 |
| NFR-S-03 | 凭据加密存储不得明文 | 账号凭据、代理凭据等敏感字段加密落库（DR-02M/DR-08） |
| NFR-S-04 | Agent 签名校验防伪造，指令签名 | 设备接入签名校验；下发指令签名 |
| NFR-S-05 | 脚本沙箱与权限白名单 | 脚本引擎沙箱执行，越权访问敏感路径拦截 |
| NFR-S-06 | 操作审计可追溯 | MC 可观测性模块全量审计日志（FR-MC-008） |
| NFR-S-07 | 文件签名 URL 限时有效 | 对象存储接口（EI-05）下发限时签名 URL |

### 7.2 保密性设计（NFR-C）

| 编号 | 需求 | 设计措施 |
| --- | --- | --- |
| NFR-C-01 | 多组织/多租户数据隔离，禁止跨组织 | COM 组织隔离，数据层按组织范围过滤 |
| NFR-C-02 | 最小权限，按角色与数据范围控制 | COM RBAC 权限决策，结合数据范围 |
| NFR-C-03 | 敏感数据脱敏展示 | 手机号、凭据等展示脱敏 |
| NFR-C-04 | 数据库定期备份，导出审计留痕 | 定期备份策略；导出操作记录审计 |

---

## 8 性能设计

### 8.1 性能指标设计（NFR-P）

| 编号 | 指标 | 设计措施 |
| --- | --- | --- |
| NFR-P-01 | 单设备远控端到端延迟 ≤ 300ms | WebRTC SFU 单节点并发多路；弱网自动降帧；远控通道与任务执行通道分离 |
| NFR-P-02 | 单租户 ≥ 200 台设备并发接入 | MC 服务端高可用部署，无单点故障；连接层水平扩展 |
| NFR-P-03 | 爬虫集群日处理能力可水平扩展 | DC 爬虫 K8s 容器化，横向扩缩容与节点自愈 |
| NFR-P-04 | 任务入队到分发 ≤ 2 秒，支持优先级与最大并发 | MC 任务管理基于 Redis ZSet 优先级队列与 Kafka 分发 |
| NFR-P-05 | WebRTC SFU 单节点并发多路，弱网降帧 | SFU 部署与降帧策略 |
| NFR-P-06 | 日志检索 ≤ 3 秒，覆盖近 7 日 | OM 统一日志库（ES/Loki）索引与分页 |

### 8.2 可靠性设计（NFR-R）

- 核心服务可用性 ≥ 99.5%（NFR-R-01）：关键服务高可用部署，无单点故障（NFR-R-04）。
- 任务幂等与失败重试，异常自动熔断（NFR-R-02）：MC 任务管理状态机 + 执行网关熔断。
- Agent 掉线自动重连，设备重启 Agent 自启动（NFR-R-03）。
- 数据采集补偿与断点续采（NFR-R-05）：DC 任务回池重排（status 回 pending，重启续跑）。

### 8.3 可维护性设计（NFR-M）

- 模块化、服务化设计，子系统可独立部署升级（NFR-M-01）。
- 采集、调度、频率控制、代理等关键策略可配置，无需修改代码（NFR-M-02）。
- 全链路日志与追踪，故障可定位（NFR-M-03）：OM 统一日志 + 任务级 trace。
- 运维仪表盘与告警，常态化运维（NFR-M-04）。

### 8.4 故障处理设计（NFR-F）

- 任务异常自动熔断、停止后续执行并告警（NFR-F-01）。
- 设备/Agent 异常自动隔离，不再派发任务，待恢复重新接入（NFR-F-02）。
- 账号封禁/受限自动停止相关任务并告警（NFR-F-03）。
- 消息中间件/数据库故障重试与死信处理（NFR-F-04）。
- 服务异常降级，优先保障远程控制与日志通道（NFR-F-05）。

---

## 9 需求追踪

### 9.1 功能需求 → 模块追踪

| 功能需求 | 软部件（模块）编号 | 子系统模块 | 设计章节 |
| --- | --- | --- | --- |
| FR-DC-001 | M-DC-01/02/03 | 任务池与调度、监控告警、爬取工程基础设施 | 4.1.3.1/4.1.3.2/4.1.3.3 |
| FR-DC-002 | M-DC-04 | 社交媒体数据采集 | 4.1.3.4 |
| FR-DC-003 | M-DC-05 | 数据处理与存储 | 4.1.3.5 |
| FR-DC-004 | M-DC-06 | 数据分析与情报 | 4.1.3.6 |
| FR-DC-005 | M-DC-07 | 情报驱动采集与目标扩散 | 4.1.3.7 |
| FR-MC-001 | M-MC-01 | 设备管理 | 4.2.1 |
| FR-MC-002 | M-MC-02 | 远程控制 | 4.2.2 |
| FR-MC-003 | M-MC-03 | 执行网关与双执行模式 | 4.2.3 |
| FR-MC-004 | M-MC-04 | 账号维护与管理 | 4.2.4 |
| FR-MC-005 | M-MC-05 | 内容管理 | 4.2.5 |
| FR-MC-006 | M-MC-06 | 任务管理 | 4.2.6 |
| FR-MC-007 | M-MC-07 | 脚本引擎 | 4.2.7 |
| FR-MC-008 | M-MC-08 | 可观测性 | 4.2.8 |
| FR-MC-009 | M-MC-09 | 设备环境与风控对抗 | 4.2.9 |
| FR-OM-001 | M-OM-01 | 日志与指标汇聚 | 4.3.1 |
| FR-OM-002 | M-OM-02 | 告警与运维 | 4.3.2 |
| FR-COM-001 | M-COM-01 | 用户认证与权限 | 4.4.1 |
| FR-SWM-001 | M-SWM-01 | 智能体账号人设管理 | 4.5.1 |
| FR-SWM-002 | M-SWM-02 | 蜂群任务智能分发 | 4.5.2 |
| FR-SWM-003 | M-SWM-03 | 行为仿真与防伪装 | 4.5.3 |
| FR-SWM-004 | M-SWM-04 | 蜂群作业统计与复盘 | 4.5.4 |
| FR-SWM-005 | M-SWM-05 | 蜂群提示词管理 | 4.5.5 |
| FR-SWM-006 | M-SWM-06 | 蜂群记忆管理 | 4.5.6 |
| FR-IRS-001 | M-IRS-01 | 本地大模型部署 | 4.6.1 |
| FR-OCC-001 | M-OCC-01 | 目标识别与维护 | 4.7.1 |
| FR-OCC-002 | M-OCC-02 | 行动方案实施 | 4.7.2 |
| FR-OCC-003 | M-OCC-03 | 效果评估 | 4.7.3 |
| FR-OCC-004 | M-OCC-04 | 舆论作业策略库 | 4.7.4 |
| FR-SR-001 | M-SR-01 | 账号资源 | 4.8.1 |
| FR-SR-002 | M-SR-02 | 内容资源 | 4.8.2 |

### 9.2 非功能需求 → 设计追踪

- 性能 NFR-P-01～06 → 第 8.1 节性能指标设计。
- 安全 NFR-S-01～07 → 第 7.1 节安全性设计。
- 保密 NFR-C-01～04 → 第 7.2 节保密性设计。
- 可靠 NFR-R-01～05 → 第 8.2 节可靠性设计。
- 可维护 NFR-M-01～04 → 第 8.3 节可维护性设计。
- 故障处理 NFR-F-01～05 → 第 8.4 节故障处理设计。

### 9.3 约束条件 → 设计追踪

| 约束 | 设计体现 |
| --- | --- |
| CON-01/02 | MC MVP 优先云手机与真机、远控与基础任务；不自研云手机内核 |
| CON-03 | DC/ETL 复用 Kafka/Spark/HBase |
| CON-04 | SWM/IRS/OCC 本期新增，优先核心闭环 |
| CON-05 | 目标海外平台，网络代理与合规 |
| CON-06 | IRS 私有化部署，第 4.6 节 |
| CON-07 | 动作收口，第 3.4/4.2.3/5.3 节 |
| CON-08 | 双执行模式，第 4.2.3 节 |
| CON-09 | 设备风控分级，第 4.2.1 节 |
| CON-10 | 推理执行分离，第 3.4/4.2.3/5.3 节 |
| CON-11 | BotBrowser 定制 + CDP + 指纹固化，第 4.2.9 节 |

### 9.4 接口/数据 → 设计追踪

- 外部接口 EI-01～06 → 第 5.1 节。
- 内部接口 II-01～16 → 第 5.2 节。
- 数据 DR-01～11 → 第 6.2 节。

---

## 10 附录

### 10.1 子系统代号对照

| 代号 | 全称 | 含义 |
| --- | --- | --- |
| DC | Data Crawler | 数据爬取 |
| MC | Multi-device Control | 多设备群控（移动端） |
| AM | Account Management | 账号管理（桌面端反指纹浏览器与账号资源执行方） |
| OM | Ops Monitor | 运维监控 |
| COM | Common | 公共功能 |
| SWM | Swarm | 蜂群智能体 |
| IRS | Inference Service | 私有化大模型部署（底层推理基础设施） |
| OCC | Opinion Combat Command | 舆情作战业务 |
| SR | Shared Resource | 共享资源域 |

### 10.2 需求与软部件编号说明

- `FR-XX-NNN`：功能需求，XX 为子系统代号（DC/MC/OM/COM/SWM/IRS/OCC/SR）。
- `NFR-P/S/C/R/M/U/F-N`：性能/安全/保密/可靠/可维护/易用/故障处理非功能需求。
- `EI-NN`：外部接口；`II-NN`：内部接口；`DR-NN`：数据；`CON-NN`：约束条件。
- `M-XX-NN`：软部件（功能模块）标识编号，遵循 GJB 438C。`M-XX-00` 为一级软部件（子系统本身），`M-XX-NN`（NN=01,02,…）为该子系统下的二级软部件（功能模块）。一条功能需求可对应一个或多个模块（按职责内聚程度拆分），功能需求与模块的对应关系见第 9.1 节追踪表。XX 取值同子系统代号。

### 10.3 待后续补充事项

1. 智能体控制的端侧 Agent 协议与策略 schema，待详细设计阶段补充。
2. 各目标平台采集与逆向能力的详细接口规格，随研发进展补充。
3. 详细数据库设计与 API 设计，见详细设计文档。
4. 舆情作战子系统策略库的初始内容与作战场景库，待业务侧补充。
5. 数据爬取子系统的任务驱动采集、监控告警与数据质量的更细化设计（任务池表结构、告警阈值矩阵、指纹算法参数等）见《软件概要设计简述》，本文档给出模块级设计。
